# CLAUDE.md — אֶפִי (Effie) / Family Bot — כללי עבודה מחייבים

> מסמכי הבסיס (לקרוא לפני עבודה על מסכים): `docs/handoff/effie-claude-code-prompt.md` (המפרט),
> `docs/handoff/effie-supabase-sync-map.md` (מיפוי מסך→טבלה), `docs/handoff/design/effie-design.dc.html` (מקור האמת הוויזואלי).

## ⚠️ מצב השקה — WHITE-LABEL בלבד
המותג "אֶפִי" **עדיין לא נחשף**. קבוע `BRAND_REVEAL=false`:
- כותרת עליונה, מסך כניסה, לוגו ← מציגים את **המשרד**: `offices.name` ("רימקס פמילי") + לוגו מ-`offices.settings.logo_url`.
- כש-`BRAND_REVEAL=true` ← המותג אפי (שם + לוגו האוריגמי `docs/handoff/effie-logo.svg`) בכל מקום.
- חריגים שנשארים ניטרליים גם אחרי החשיפה: סיכומי AI ← **"סיכום חכם"**; מסך הצ'אט ← **"העוזר החכם"** (לא "אפי סיכם").
- שם המוצר (לאחר חשיפה): **אֶפִי** (בלי דגש בפ'). שם המשרד תמיד מ-`offices.name` — **לעולם לא hardcoded**.

## 🎨 עיצוב (קבוע — אין להוסיף צבעים/סגנונות)
- **צבעים:** קרם `#F2EFE7` (רקע) · נייבי `#1E3A5F` (כותרות/טאב פעיל; גרדיאנט `#0E1D33→#2C4C77`) · כחול פעולה `#2E6BD6` · זהב `#C29435`/`#E4C56B` (AI/החתמה/נכס נולד) · ירוק `#1FAF5E` (וואטסאפ/טלפון/נחתם) · אדום `#C24040` (**מחיקה בלבד**).
- צ'יפים: זהב `#F6EEDB` · כחול `#EAF0FA` · ירוק `#E7F7EE` · אדום `#FBEDED` · אפור `#F0EDE3`. טקסט משני `#8B8F99`/`#5B6472`, גבולות `#E9E4D8`/`#DCD6C8`.
- **פונט Heebo (400–800), RTL מלא.** כרטיסים לבנים, radius 20-22, צל `0 6px 20px rgba(30,58,95,.06)`.
- **היררכיית כפתורים:** כחול=ראשי · זהב=הדגשה/AI/החתמה · ירוק=וואטסאפ/טלפון · לבן+מסגרת `#DCD6C8`=משני · אדום=מחיקה (אייקון פח 42×42 על `#FBEDED` בלבד, לעולם לא כפתור מלא).
- מינימום מגע 44px · **אין אימוג'י ב-UI** (לוגו וקטורי בלבד) · אין מסכי-ריק יבשים (אייקון עיגול זהב 72px + כותרת + הסבר + CTA).
- **ניווט תחתון, מימין לשמאל:** שיחות · קונים · **בית** (אריח נייבי מורם 44×44 radius 15, טבעת זהב כשפעיל) · חתימות · נכס נולד (לוגו + badge מונה). "נכסים" ו"תהליכים ועסקאות" — ממסך הבית ומתפריט הצד, לא טאבים.

## 🗄️ נתונים וארכיטקטורה
- **מחירים/תאריכים נשמרים כטקסט גולמי** (parity עם הגיליון) — פורמט (₪, פסיקים, dd/mm) בצד לקוח בלבד.
- **כתיבה ל-DB דרך השרת בלבד** (service_role). קליינט = קריאה + Realtime בלבד (JWT עם claim `office_id`, RLS אוכף).
- **מולטי-טננט:** הכל מסונן `office_id`. RE/MAX Family = `11111111-1111-4111-8111-111111111111`.
- **טבלת המשתמשים הקיימת היא `users`** (לא `agents` כפי שכתוב במפרט §6) — role: admin/coordinator/agent. להשתמש בקיים.
- טבלאות קיימות (אל תיגע בסכימות): offices, users, newborn_listings/status/notes/contacts, calls, hidden_calls, signatures, buyers (+RPC buyers_delete_row), external_exclusives, properties, office_config.
- חוסרים שנותרו להוסיף (מיגרציות חדשות בלבד): `buyers.status`, `deals`, `announcements`+`announcement_reads`, `users.coordinator_id`, `activity_log`, ובהמשך `daily_briefs`.
- **הרשאות תצוגה בכל מסך:** מנהל=כל המשרד · מתאמת=הסוכנים המשויכים לה · סוכן=רק שלו.

## 🔧 כללי עבודה בריפו (חובה — נלמדו בדם)
- **הקובץ הקנוני:** `/Users/eyal/Documents/GitHub/remax-bot/app.py`. יש עותקים ישנים במקומות אחרים — **תמיד נתיב מוחלט**, לעולם לא נתיב יחסי (ה-cwd מתאפס בין פקודות).
- אימות אחרי כל שינוי: `python3 -c "import ast; ast.parse(open('<נתיב מלא>/app.py').read())"` + חילוץ JS מבלוקי HTML שהשתנו (ב-effie_v2.py) ו-`node --check` + יבוא מודול אמיתי (דורש env דמה: MAYTAPI_TOKEN/MAYTAPI_PHONE_ID/MAYTAPI_PRODUCT_ID/CLAUDE_API_KEY).
- **סודות ב-Render env / Script Properties בלבד** — לעולם לא בקוד או ב-git. קובצי קוד עם טוקנים (קוד.gs המלא) — scratchpad בלבד, לא בריפו.
- לא לגעת ב-MOBILE-PATCH (fbAutoLogin/fbDoBio/fbShowLock) ולא ב-whatsapp:// של הסוכן.
- דגלי מקור נתונים (Render env): NEWBORN/CALLS/SIGNATURES/BUYERS/EXCL/PROPS/CONFIG_SOURCE = sheets|supabase. הגיליון עדיין מקור אמת לכתיבות (דרך Apps Script, עם כתיבה כפולה + סנכרון-השלמה כל דקה/30ד').
- דפלוי: Eyal בלבד (GitHub Desktop → Render Manual Deploy). פריסות Apps Script: 2-3 דקות הבהוב אחרי — רק בשעות שקטות.
- האפליקציה המשרתת סוכנים בפרודקשן היא `/v2` (effie_v2.py) — **אסור לשבור אותה**. (v1/FAMILY_BOT_HTML נמחקה סופית 15/07/2026.)

## 📓 יומן החלטות (להוסיף כל החלטה חדשה מיד)
- 2026-07-05: סשן 0 בוצע — handoff ב-docs/handoff/, קובץ העיצוב נקרא effie-design.dc.html.
- 2026-07-05: **הוחלט (אייל): אפי חיה במסלול /v2 באותו שרת**, במקביל לאפליקציה הקיימת.
- 2026-07-05: סשן 1 בוצע — מימוש ב-**מודול נפרד `effie_v2.py`** (וניל, אותו שרת); app.py נוגע במינימום:
  רישום `effie_v2.register(app, globals())` עטוף try/except לפני STARTUP + פרמטר `next=v2` לזרימת Google
  (חזרה ל-/v2/home במקום /app). אותם טוקנים/סשנים (fbTok) לשתי האפליקציות — כניסה אחת.
- 2026-07-05: מיגרציות החוסרים בקובץ חדש `supabase_schema_effie.sql` (buyers.status, deals,
  announcements+announcement_reads, users.coordinator_id, activity_log) — **ממתין לאייל להריץ ב-SQL Editor**.
- 2026-07-05: מסכי /v2 (כניסה), /v2/home (זמני עד סשן 2), /v2/admin (ניהול 31a). הניהול משתמש ב-API הקיים
  (/api/dev/people, /api/dev/role, /api/dev/coordinators, /api/dev/suspend) + חדשים תחת /v2/api/*
  (overview, invite בוואטסאפ, policy, office). מדיניות נשמרת בקונפיג תחת `v2_policies`
  (transcribe, shtaf_sharing, share_buyers, require_followup, who_contacted_admins_only) — האכיפה במסכים הבאים.
- ממתין לאישור/סשנים הבאים: אכיפת המדיניות במסכים ·
  "פעילה עכשיו" בשורת חבר צוות (דורש presence) · חיבור מסך /v2/admin ל-users בסופאבייס במקום הקונפיג (אחרי המעבר).
- 2026-07-05: המיגרציות הורצו ואומתו ב-Supabase ע"י אייל (4 טבלאות + 2 עמודות).
- 2026-07-05: סשן 2 בוצע — מסך הבית (14a) + בריף הבוקר (13a) ב-/v2/home בתוך effie_v2.py:
  סטורי 4 כרטיסים (פתיחה ← קונים בלי שיבוץ ← חתימות ← נכס נולד), נפתח פעם ביום (localStorage
  `v2BriefSeen` — בשלב זה, daily_briefs יגיע עם ה-Edge Function), דשבורד נטען ברקע בזמן הסטורי.
  נתונים מ-API קיימים בלבד: /api/report?period=week, /api/my/buyers, /api/my/properties, /api/newborn
  (badge המונה בניווט כבר מחובר מכאן). תפריט צד מינימלי (ניהול/אפליקציה קיימת/התנתקות) — הגרסה
  המלאה בסשן התפריט. טאבים/פעולות שטרם נבנו מציגים toast "בסשן הקרוב" — להחליף בקישורים אמיתיים בסשנים 3–8.
- 2026-07-05: דסקטופ (בקשת אייל, אחרי צילום מסך רחב): כל מסכי /v2 מוגבלים לעמודה ממורכזת 600px
  ב-media query‏ ≥700px (כולל sheets, תפריט וסטורי הבריף). המובייל ללא שינוי; דסקטופ מלא (סרגל צד,
  גריד) — בשלב הדסקטופ לפי מפרט §13.
- 2026-07-05: מיתוג (בקשת אייל): בכל מסכי /v2 — לוגו המשרד ממורכז בכותרת **בלי טקסט שם המשרד**
  (גם במסך הכניסה — הלוגו בטבעת בלבד). השם נשאר רק ב-document.title, בהודעות וואטסאפ ובשדה העריכה בניהול.
- 2026-07-06: קונים — **חיפוש חכם** (בקשת אייל): כפתור "חפש" (זהב) + Enter מריצים /api/search/buyers
  (סריקת שיחות שנענו עם הבנת תקציב ומילות מפתח); תוצאות עם צ'יפ תקציב, סיכום, "הוסף כקונה" ממולא
  מראש, וואטסאפ/חיוג + "נקה חיפוש". הקלדה בשדה = סינון מקומי כרגיל. שורת החיפוש הודקה שלא תיחתך.
- 2026-07-06: דוחות — "החודש" פותח בורר חודשים מתחילת השנה (צ'יפים, ברירת מחדל החודש הנוכחי,
  /api/report?month=1..12 הקיים; התווית מהשרת "מרץ 2026").
- 2026-07-06: דוחות — ירד KPI "מודעות פעילות" (בקשת אייל). נשארו 8: שיחות, נענו+%, חתימות, בלעדיות,
  עסקאות בתקופה, פגישות ופולו-אפ, תהליכים פתוחים, עסקאות השנה.
- 2026-07-06: (בקשות אייל) נכס נולד: "לא מעוניין" הוא מתג — לחיצה נוספת מסירה את הסטטוס
  (/api/newborn/status/delete), הכפתור אדום-מלא כשפעיל. בית "דורש טיפול": כולל גם פגישות/פולו-אפים
  עתידיים (צ'יפ תאריך אפור), ממוין באיחור→היום→מחר→עתיד. הערה: /api/report במטמון שרת 120ש' —
  פגישה חדשה עשויה להופיע בבית באיחור של עד 2 דק'.
- 2026-07-06: **מהירות — פתיחה מיידית (SWR):** כל 6 הטאבים המרכזיים (שיחות/קונים/חתימות/נכס
  נולד/תהליכים/נכסים) נפתחים מיד מהעותק האחרון ב-localStorage (v2c:*, חתוך לגודל בטוח) והרענון
  מהשרת רץ ברקע. בפרודקשן: 5/7 מקורות כבר על Supabase (בדיקת /api/dev/sources) — נשארו
  נכסים+שת"פ על sheets; המעבר שלהם (PROPS/EXCL_SOURCE=supabase אחרי parity_check) = הצעד הבא למהירות שרת.
- 2026-07-06: (בקשות אייל) ניהול: רשימת הצוות מציגה 5 בלבד + שורת "+ עוד N — חפש לפי שם";
  חיפוש חושף את כולם. קונים: ירד כפתור "חפש" — החיפוש החכם נשאר ב-Enter (מרומז ב-placeholder).
- 2026-07-06: חבילת תיקונים (רשימת אייל): נעילת גלילת רקע כשגיליון פתוח (7 מסכים) · הניווט מוסתר
  כשמקלדת פתוחה (10 עמודים) · התאמות שת"פ: כתובת (street) + תיאור + תאריך פרסום · אחוזי התאמה:
  שאילתה נקייה (דרישות+תקציב) + מיון לפי score · מניעת הוספת קונה כפולה (busy guard, 2 מסכים) ·
  מחיקת קונה (פח בעריכה, /api/buyers/delete) + עריכה ברורה יותר + load() אחרי שמירה · דף בית:
  סטטיסטיקה אמצעית = "גויסו השבוע" · דוחות: ירד KPI נכס-נולד, נוספו "תהליכים פתוחים"+"עסקאות השנה",
  מובילים 10 בשרת (topGius/topKonim/top_deals) · נכס נולד מוקשח (try/catch פר כרטיס) · טרוקולר
  בשיחות (truecaller.com/search/il) · סיכת המשרד במפה (שושנה דמארי 4 — 32.85042,35.08606).
- 2026-07-06: **שני מסכים חדשים:** /v2/updates — עדכונים למשרד (31b v1) על announcements+reads
  מ-Supabase (endpoints /v2/api/ann*): מנהל/מתאמת מפרסמים+נעיצה, סוכן "אישרתי·קראתי", מנהל רואה
  "אישרו N"+מחיקה; /v2/activity — יומן שימוש (9ו) על /api/activity הקיים, מנהל בלבד, רענון 45ש'.
  קישורי תפריט: עדכונים (לכולם), יומן שימוש (מנהל). פתוח: badge לא-נקראו בתפריט · "מי קרא" שמי ·
  עדכוני מערכת אוטומטיים.
- 2026-07-06: ביצועים+UI (דיווחי אייל): (1) שיחות — רינדור מיידי מהבקשה הראשית; מוסתרות+קונים ברקע;
  רענון תקופתי לראשי בלבד; מצב "טוען…" (נמדד: history קר 6ש', חם 0.8ש' — ההצעה: CALLS_SOURCE=supabase
  ב-Render env יוריד לקר <1ש', יש parity_check). (2) כל הגיליונות הנפתחים נעצרים מעל הניווט (nav z=40,
  sheet bottom=74px+safe) — הטאבים גלויים תמיד, לפי המפרט.
- 2026-07-06: באג-פיקס בריף: v2BriefSeen נכתב רק בסגירת הסטורי — רענון/סגירת טאב בלי X הקפיצו אותו
  שוב ושוב (הדיווח של אייל בספארי). תוקן: הסימון נכתב מיד בפתיחה האוטומטית; "צפה שוב" זמין תמיד מהפס.
- 2026-07-06: חקירת "יקינטון 24ד לא מופיע": החתימה קיימת אך מסוג **"מוכר"** (הסכם מכירה), לא
  "בלעדיות" — לכן לא נספרה בכרטיס הגיוס בבריף. תוקן: כרטיס הגיוס סופר עכשיו **כל החתמת בעל נכס**
  (מוכר+בלעדיות, "בלעדיות" מצוין בשורה) + דה-דופ לפי כתובת+חותם (נמצאו רשומות כפולות בגיליון
  החתימות — כדאי לבדוק במקור). פילטר "בלעדיות" במסך החתימות נשאר בלעדיות-בלבד בכוונה.
- 2026-07-06: קונים (בקשת אייל): (1) אייקון להבה בכרטיס — מחליף חם⇄פעיל בלחיצה (זהב מלא כשחם);
  (2) עיפרון עריכה → גיליון: סטטוס + "מה מחפש" (search — השדה היחיד ש-updatebuyer ב-Apps Script
  תומך בו; שם/טלפון/תקציב ייפתחו לעריכה עם המעבר ל-Supabase, כתוב במסך). כפתור ההתאמה קוצר ל"התאם".
- 2026-07-06: סשן 9א בוצע — דוחות /v2/reports (30a): פילטר זמן (lastweek/week/month/year), 8 KPI
  (שיחות, נענו+%, חתימות, בלעדיות, מודעות, עסקאות-בתקופה מ-/api/deals לפי close_date, פגישות, נכס
  נולד), מובילים מאוחד עם צ'יפים + פילוח עסקאות לפי צד + "כל 10", שת"פ (המשרד בזהב), נכס נולד לפי
  ערים, ייצוא וואטסאפ (wa_text מהשרת)/PDF (window.print עם print CSS)/העתק. גרסת סוכן מסוננת בשרת.
  תפריט הצד בבית קיבל: דוחות, תהליכים ועסקאות, נכסים במפה. חסר עדיין: "% פגישות שהתקיימו" (אין דאטה).
- 2026-07-06: **באג-פיקס מתאמת (חשוב):** הסקופ (agents/agent_names) נקבע רק בכניסה ונשמר בסשן —
  שיוך סוכנים שנעשה בניהול אחרי שהמתאמת מחוברת (או תפקיד מתאמת שניתן לסשן חי) לא נתפס עד לוגין מחדש.
  תוקן ב-`_refresh_coordinator_scope()` שנקרא מ-`_web_auth` **בכל בקשה**: מרענן שיוך חי מהקונפיג,
  משדרג סוכנת→מתאמת ומחזיר מתאמת שהוסרה→סוכנת. מכסה את כל המסכים (שיחות/קונים/חתימות/דוחות/פגישות/עסקאות).
- 2026-07-06: ניווט (בקשת אייל, דורס את 14a): **האריח הנייבי המורם (44×44) מסמן את הטאב הפעיל** בכל
  מסך — לא רק את הבית; הבית מוצג כאייקון רגיל כשאינו פעיל. טבעת הזהב ירדה. במסכים שאינם טאב
  (נכסים/תהליכים/ניהול) — אף טאב לא מורם. badge נכס נולד נשאר רק בבית.
- 2026-07-06: סשן 8 בוצע — תהליכים ועסקאות /v2/deals (22a + טפסים 24a/27a): רשימה עם חיפוש,
  פילטרים פתוחים/נסגרו/הכל, כרטיס עם צ'יפ שלב, "נמכר ← עסקה" (זהב)/עריכה/פח+אישור. טופס עסקה:
  מבוקש line-through מול מכירה בזהב, תאריך, עמלה אוטו' 2%+מע"מ 18% עם עיפרון לידני, עו"ד, CTA דביק.
  על /api/deals הקיים (אחסון מקומי deals.json — יעבור ל-Supabase deals בהמשך המיגרציה); app.py הורחב
  אדיטיבית לשמור stage/commission/commission_manual. צירוף חוזה חתום — כש-Storage יחובר. חיווט מהבית.
- 2026-07-06: סשן 7ב בוצע — מפת הנכסים /v2/map (25a): Leaflet+markercluster מ-CDN, פיני מחיר
  (משרד נייבי/שת"פ כחול/נבחר זהב), קלאסטרים נייבי, פילטרים צפים, חיפוש, "המיקום שלי", כרטיס צף עם
  צ'יפי מאפיינים מזוהים מהתיאור + מחיר למ"ר, CTA שנוחת ב-/v2/props עם חיפוש ממולא (דרך v2st:props).
  נתונים: /api/map/properties הקיים (geocache). שכבת נכס נולד — כשיהיה geocode לכתובות; שאר החוסרים
  הבאים: פגישות/פולו-אפ מלא (21a), תהליכים (22a), טפסי החתמה, דוחות (30a), עדכונים, AI (25b), תפריט (23a).
- 2026-07-06: **TestFlight** — עטיפת ה-Capacitor (ריפו remax-family המקומי) הופנתה ל-/v2
  (capacitor.config.json + cap sync בוצע), רקע קרם. מסך הכניסה של v2 קיבל תמיכת נייטיב:
  כפתור Google עובר ל-?native=1 בתוך Capacitor + מאזין appUrlOpen לדיפ-לינק remaxfamily://login?token
  (כמו MOBILE-PATCH של /app). צ'קליסט בילד/העלאה: remax-family/TESTFLIGHT.md. אותו bundle id —
  בילד חדש מחליף את /app לבודקי TestFlight (אפשרות לאפליקציה נפרדת מתועדת שם). Face ID ב-v2 — בהמשך.
  **חובה דפלוי remax-bot לפני הבילד.**
- 2026-07-06: אומת מול פרודקשן (בקשת אייל): כל 433 נכסי המשרד + 30 שת"פ נשאבים מהשיטס
  (fetch_sheet_rows/external_exclusives) — כולל נכסים שהוזנו ידנית בגיליון, אותו מקור ל-v2.
- 2026-07-06: סשן 7א בוצע — טאב הנכסים המאוחד /v2/props (20a): מתג משרד/שת"פ/שלי עם מונים, חיפוש
  חכם דרך search/properties+exclusives (debounce, score), "קונים מתאימים" (search/buyers — שידוך הפוך
  מהשיחות שנענו, עם וואטסאפ/חיוג), "פרטי הנכס", שת"פ מעומעם + אותה-רשת מודגש (היוריסטיקת 'רימקס' עד
  שיש offices.network), שלי במסגרת זהב + ?mine=1 מפס הבית. זיכרון מצב v2st:props. **המפה — סשן 7ב**.
- 2026-07-06: בריף — כרטיס החתימות (3/4) מסכם עכשיו **בלעדיות שגויסו ב-7 הימים האחרונים**:
  מספר גדול בזהב "נכסים חדשים גויסו השבוע" + עד 4 כתובות עם הסוכן המגייס, וסה"כ החתימות בשורת משנה.
  אם לא גויסו — נשאר כרטיס החתימות הכללי. הנתונים מ-/api/signatures (סינון "בלעדיות" + ts).
- 2026-07-06: **שליחות וואטסאפ אוטומטיות מושהות** (בקשת אייל — שוקל מעבר ל-API רשמי): התראת שיחה
  חדשה (סוכן+קבוצה) והתראת חתימה (סוכן+קבוצה) מאחורי `_wa_auto_on()` — כבוי כברירת מחדל, מתג
  "וואטסאפ אוטומטי מהמערכת" בניהול /v2 (v2_policies.wa_auto). שיחות שלא נשלחו מסומנות כנצפו —
  אין הצפת עבר בהפעלה מחדש. **לא הושפעו** (="מבחירה"): תשובות הבוט לפקודות בוואטסאפ, שליחת חוזה
  לחתימה ע"י סוכן, קישורי wa.me מהמסכים.
- 2026-07-06: באג-פיקס חתימות: הפילטר "קונים" סינן לפי "מתעניין" אבל _deal_label בשרת מחזיר
  "קונים"/"בלעדיות"/"שכירות"/"מוכר" — הסינון תוקן לתוויות האמיתיות; צ'יפ אפור לסוגים אחרים.
- 2026-07-06: ניווט תחתון מקובע (position:fixed) בכל מסכי /v2 + **זיכרון מצב פר-טאב** (localStorage
  v2st:*): שיחות (פילטר+מוסתרות+גלילה), קונים (פילטר+חיפוש+גלילה), חתימות (פילטר+גלילה),
  נכס נולד (ותק+חיפוש+גלילה) — חוזרים לטאב בדיוק איפה שעזבת.
- 2026-07-05: חתימות — פילטרים בסדר חדש (בקשת אייל): **קונים** (מימין, ברירת מחדל) · **בלעדיות** ·
  **הכל** (בסוף — שכירות ובעלי נכס רגילים מופיעים שם). ירד פילטר "בעלי נכס" הנפרד.
- 2026-07-05: ניהול (בקשות אייל): (1) מחיקת סוכן — פח 42×42 על #FBEDED בגיליון החבר + אישור,
  על /api/dev/agent_delete הקיים (מנקה צוותים/מתאמות/תפקיד וחוסם כניסה); (2) בחירת "מתאמת" בגיליון
  חושפת מיד את בורר הסוכנים (ושינוי תפקיד ממתאמת מסיר את השיוך); (3) לוגו מסך הכניסה + טבעת הבריף —
  על עיגול לבן מלא בתוך טבעת הזהב.
- 2026-07-05: סשן 6 בוצע — נכס נולד /v2/newborn (19a): מונה חי (פולינג 60ש' עד Realtime), צ'יפי ותק
  חודש 1–7+ (bucketCounts מהשרת, תואם _NB_BUCKETS), חיפוש, כרטיס עם שורת בעל הנכס (חייג/וואטסאפ →
  נרשם "פנה" דרך /api/newborn/contact; צפייה במודעה), "כבר פנו" למנהל/מתאמת בלבד, סטטוסים
  פגישה/פולו-אפ (datetime → יומן Google)/לא-מעוניין/הערות משותפות (endpoints קיימים), וגיליון
  "פגישות ופולו-אפ" מ-/api/newborn/meetings עם חיוג/וואטסאפ. **כל חמשת הטאבים מחווטים — אפס "בסשן הקרוב"**.
- 2026-07-05: ניהול — שדה חיפוש סוכן בכרטיס הצוות (שם/טלפון/וירטואלי); הכותרת מציגה "N מתוך M" בסינון.
- 2026-07-05: סשן 5 בוצע — מסך החתימות /v2/sigs (17a): "החתם בעל נכס" (זהב) + "החתם מתעניין" (משני),
  פילטרים הכל/בעלי-נכס/מתעניינים/בלעדיות, כרטיס עם צ'יפ סוג, נקודת סטטוס (נחתם=יש link/pct; אחרת ממתין)
  ו"צפייה במסמך החתום" (commission_pct כ-URL בזרימת SMS). על /api/signatures הקיים. **טפסי ההחתמה בקו
  אפי (סבב 18 בעיצוב) — סשן נפרד**; בינתיים הכפתורים מפנים לזרימה הקיימת ב-/app. חיווט טאב חתימות בכל
  המסכים + "החתם" מהבית + "החתם מתעניין" בתוצאות ההתאמה + CTA כרטיס 3/4 בבריף.
- 2026-07-05: סשן 4 בוצע — מסך הקונים /v2/buyers (16a) + התאמת נכסים (16b): חיפוש, "+ קונה"
  (/api/buyers/add), פילטרים לפי buyers.status ("פעילים" כולל חמים; החלפת סטטוס בלחיצה על הצ'יפ —
  endpoints חדשים /v2/api/buyers/status[es] קוראים/כותבים ישירות ל-Supabase עם service_role).
  קונה חם = מסגרת זהב + תג. "התאם נכסים" = search/properties + search/exclusives על דרישות הקונה
  (ה-score של המנוע הקיים) — משרד קודם, שת"פ מעומעם עם שם משרד, "שלח לקונה" בוואטסאפ עם הודעה מוכנה,
  "החתם מתעניין" יחובר בסשן החתימות. חיווט: טאב קונים בכל המסכים, "הוסף קונה" מהבית (/v2/buyers?add=1),
  CTA הבריף "שבץ אותם עכשיו" → /v2/buyers.
- 2026-07-05: סשן 3 בוצע — מסך השיחות /v2/calls (עיצוב 15a): משפך שיחות←נענו←הפכו-לקונים (הצלבת
  טלפונים מול הקונים), פילטרים הכל/נענו/לא-נענו/חמים (חם = סיכום עשיר/פרטי לקוח — היוריסטיקה עד שיש
  דגל AI), מספר וירטואלי + העתקה, "סיכום חכם" (white-label), תמלול/פרטי לקוח בגיליון, "הוסף כקונה"
  בכל שיחה (/api/buyers/add), זיהוי חוזר (מספר שקיים כקונה → שם + תג "קונה קיים"), הסתרה/שחזור
  (/api/calls/hide+unhide) עם תצוגת מוסתרות. הכל על /api/history הקיים; רענון כל 90ש' עד Realtime. מבליט את הנכסים שנולדו ביממה האחרונה (ageDays=0):
  מספר גדול בזהב + עד 4 כתובות ("+ עוד N"), סה"כ הפול בשורה משנית; אם לא נולדו — נשאר מסך הפול הכללי.
  גם פס הבריף בדשבורד מציג "N נולדו ביממה האחרונה".
- 2026-07-05: ניהול — "כניסה כסוכן (בדיקה)" בגיליון חבר צוות (על /api/admin/loginas הקיים; טוקן המנהל
  נשמר ב-fbTokAdmin, פס זהב "מצב בדיקה" בבית עם "חזרה למנהל"). פרטי משרד: נוספו אינסטגרם + מדלן
  (cfg v2_office, פר-משרד ל-white-label) — עריכה בגיליון "פרטי המשרד" בניהול, מוצגים בתפריט הצד.
- 2026-07-05: שדרוגי מסך הניהול (בקשת אייל): (1) בגיליון חבר צוות — בקרת "נכס נולד: ממתי רואה מודעות"
  (ברירת מחדל של המשרד / מספר ימים מותאם / מוסתר, דרך /api/dev/agent_update newbornDelay);
  (2) הצגת המספר האישי + הווירטואלי בגיליון החבר — הווירטואלי עריך (agent_update vphone, קריאה אחת רק עם מה שהשתנה); (3) מתג "שיתוף קונים בין סוכנים" הוחלף בכרטיס
  **"צוותי שיתוף"** על מנגנון teams הקיים — חברי צוות רואים הכל אחד של השני. הרחבה ב-app.py:
  `_deals_can_see` מכבד עכשיו גם צוות (תהליכים ועסקאות; שיחות/קונים/חתימות/נכסים/דוחות כבר כובדו).
- 2026-07-06: סשן חתימות — טופס החתמה נייטיב /v2/sign (עיצוב 18a/18b) ב-effie_v2.py (V2_SIGN_HTML):
  מסך אחד לשני המסלולים לפי ?type=buyer|owner. מתעניין: קניה (אחוז עמלה, ברירת מחדל 2) / שכירות
  (חודשי עמלה); בעל נכס: מכירה / השכרה / בלעדיות (עם תאריכי מ‑עד). מיפוי מסמכים לחוזים הקיימים:
  buyer→CLIENT_SALE/CLIENT_RENT, owner→OWNER_SALE/OWNER_RENT/OWNER_EXCLUSIVE — הטקסטים נמשכים
  מ-/api/sign/contract וההחלפות (SALE_FEE, RENT_FEE, EXCLUSIVE_FROM/TO, {שם_הלקוח} וכו') זהות לאפליקציה
  הישנה. פרטי לקוח עם אימות ת"ז ישראלית חי; ריבוי נכסים — הוספה חופשית או בחירה מ-/api/sign/properties.
  שני מצבים: שליחה מרחוק (ברירת מחדל, /api/sign/send_remote — SMS+וואטסאפ, הלקוח חותם במכשירו) או
  חתימה במקום (קנבס חתימה + ת"ז חובה, /api/sign/submit). חיווט: מסך החתימות "טופס חדש" → /v2/sign,
  ו"החתם מתעניין" בתוצאות התאמת נכסים → prefill דרך localStorage v2signPre (לקוח/טלפון/כתובת/מחיר).
- 2026-07-06: תיקוני משוב מהאייפון (כולם ב-effie_v2.py): (1) **Face ID** — שער נעילה בכניסה (/v2)
  בתוך אפליקציית Capacitor בלבד (Capacitor.Plugins.NativeBiometric, הפלאגין כבר בעטיפה): מסך נעילה
  בסגנון אפי (קרם/נייבי, לוגו בטבעת זהב, בלי אימוג'י), "כניסה עם Face ID" / "כניסה עם מספר טלפון"
  (מנקה סשן); בדפדפן — כניסה ישירה כרגיל. רץ בכל קולד-סטארט כי server.url של העטיפה הוא /v2.
  (2) **בריף בכל כניסה** — הטריגר עבר מ"פעם ביום" (localStorage) ל"פעם בסשן" (sessionStorage v2BriefSess):
  נפתח בכל פתיחת אפליקציה/טאב חדש, לא ברענון או חזרה לבית באותו סשן (שומר על תיקון ה"קופץ בספארי").
  (3) **זיכרון מיקום בטאבים** — תוקן באג שדרס את המיקום השמור: כשהשחזור רץ לפני שהרשימה נטענה
  (scrollTop לא תפס), saveSt() שכתב y=0. עכשיו _restY לא נצרך עד שהשחזור מצליח, אין שמירה כשהוא
  ממתין, גלילת משתמש מבטלת שחזור ממתין, ונוסף pagehide→saveSt בכל 6 הטאבים (מעבר טאב שומר מיד).
  (4) **טרוקולר באפליקציה** — ל-iOS אין דיפ-לינק חיפוש; בנייטיב: העתקת המספר ללוח + פתיחת truecaller://
  (טרוקולר מזהה מהלוח ומציע לחפש), בדפדפן נשאר חיפוש הווב. הערה: הצילום שצורף היה מהאפליקציה הישנה —
  שם אין כפתור טרוקולר; באפי הכפתור קיים וממתין לדפלוי.
- 2026-07-06: מהירות — הפעלת cache-warmer מחדש (app.py): נמדד בפרודקשן שהמשתמש הראשון אחרי
  פקיעת מטמון משלם את המשיכה במלואה (/api/history קר 4.7ש', /api/report 3.1ש', /api/newborn 2.7ש'
  מול 0.2–0.9ש' חם), כי ה-TTL קצר (10–15ש' ל-Supabase) וה-warmer הישן כבוי (הפציץ את Apps Script).
  התיקון: (א) TTL של מקורות Supabase הועלה ל-90ש' (שיחות/חתימות raw, newborn, hidden, props);
  (ב) warmer חדש עם **רענון-מקדים** (_warm_key): מושך מפתח מחדש רק כשהוא עומד לפוג לפני המחזור
  הבא (30ש'), עם עותקים raw שמכבדים את דגלי המקור (_newborn_fetch_raw/_props_fetch_raw); מקורות
  שמנהלים מטמון בעצמם (signings_sheet, hidden, בלעדיות חיצוניות, config) נקראים דרך העטיפה —
  עבודה רק כשה-TTL פג, כלומר Sheets/Apps Script לא נקראים תכוף יותר מהיום (מה שחנק בעבר לא חוזר);
  (ג) מופעל כברירת מחדל, כיבוי עם DISABLE_WARMER=1 (הדגל הישן ENABLE_WARMER כבר לא נדרש).
  טריות נתונים: שיחות מתעדכנות תוך ≤~85ש' במקום ≤10ש' — מקובל (המסך ממילא מתרענן כל 90ש').
- 2026-07-06: שלושה תיקוני משוב: (1) **שיחות** — הוסר כפתור הטרוקולר; המספר בכרטיס הוא עכשיו
  קישור tel: — לחיצה ארוכה פותחת את תפריט המערכת של iOS (חיוג/הודעה/הוספה לאנשי קשר/העתקה — ומשם
  טרוקולר מזהה מהלוח), כמו באפליקציה הישנה. (2) **התאמות לקונה** — כרטיס שת"פ מוצג באותו פורמט של
  המשרד (כתובת, שורת פרטים קצרה מ-dest, מחיר, אחוז התאמה) במקום גוש הטקסט; בשני הסוגים נוסף אייקון
  "הרחב לתיאור" (שברון שנפתח ל-div תיאור מלא, מסתובב 180°); שורת הפרטים קוצצה ל-2 שורות (line-clamp).
  (3) **מחיקת חתימה דיגיטלית — מנהל בלבד**: /api/sign/delete ב-app.py מקבל עכשיו גם role=admin (לא רק
  מפתח); בדף החתימות נוסף פח 42px על #FBEDED (חוק העיצוב) שמופיע רק למנהל ורק על רשומות מהאפליקציה
  (עם eid — ייצוא מהקרם לא ניתן למחיקה מכאן), עם confirm ו-load() מחדש.
- 2026-07-06: בית — תג מונה (qaBadge, זהב) על אריח "תהליכים": מספר התהליכים הפתוחים (deal=false
  מ-/api/deals). ההיקף מגיע מהשרת שכבר מסנן לפי תפקיד — סוכן רואה את שלו (+צוות שיתוף), מנהל את כולם.
- 2026-07-06: הכנה להדלקת PROPS/EXCL_SOURCE: (א) ל-parity_check.py נוספה השוואת **נכסים במשרד**
  (Google Sheets API ↔ properties, שורה-שורה ושדה-שדה, כולל _desc_ae וסינון אווה אזולאי) — עד עכשיו
  הסקריפט לא בדק את הטאב שהדגל PROPS_SOURCE מדליק; (ב) endpoint חדש /api/dev/parity (מפתח בלבד)
  שמריץ את parity_check על השרת עצמו (הסודות בסביבת Render, לא עוברים למחשב) ומחזיר את הדוח +
  green. תהליך ההדלקה: דפלוי → GET /api/dev/parity → אם green: להוסיף PROPS_SOURCE=supabase +
  EXCL_SOURCE=supabase ב-Render Environment (שומר ומרנדר מחדש אוטומטית).
- 2026-07-06: parity הורץ בפרוד (/api/dev/parity): נכסים 433=433 ✅, שת"פ 342=342 ✅ — **ירוק להדליק
  PROPS_SOURCE/EXCL_SOURCE=supabase**. "16 חתימות חסרות" התבררו כארטיפקט: הכתיבה הכפולה מחתימה
  received_at בכל צד בנפרד (הפרש שניות) — מפתח ההשוואה עודן לרמת יום. בדרך התגלו כפילויות אמיתיות
  (שורות זהות לגמרי: אותו event_id+timestamp+לקוח פעמיים) — נוסף סינון בטוח ב-get_signings שמסנן
  זהות-מוחלטת בלבד (לא נוגע בהחתמות חוזרות לגיטימיות). מדידת פרוד עם ה-warmer: history 1.1/0.6ש',
  report 2.0/0.25ש' (לעומת 4.7/3.1 קר לפני). newborn ספייק 9ש' חד-פעמי סמוך לדפלוי — לעקוב.
- 2026-07-06: **שכבת דסקטופ/טאבלט** (עיצוב §13, "המובייל הוא המקור"): V2_DESKTOP_CSS מוזרק לכל דף
  ב-_page() אחרי ה-CSS של הדף (גובר במסכים רחבים), ממוקד ב-body:has(nav) — דף הכניסה לא מושפע.
  דסקטופ ≥1024: סרגל צד קבוע מימין 224px (לוגו המשרד למעלה ::before, פריטים עם תוויות, פעיל = גלולת
  נייבי via :has(.home), badge נכס נולד בזרימה), תוכן עד 940px; הבית: בריף כבאנר רוחבי + גריד שתי
  עמודות (greet/stats/brief/care פרוסים מלא, הכפתורים והנכסים-שלי בעמודות). טאבלט 768–1023: הסרגל
  מתכווץ לאייקונים (66px, font-size:0). מובייל: ללא שינוי (ניווט תחתון). הסתרת הניווט במקלדת
  (focusin) הוגבלה ל-<768px כדי לא להעלים את סרגל הצד בהקלדה. אומת ב-1400/768/375.
- 2026-07-06: **מסך פגישות ופולו-אפ** /v2/meets (עיצוב 21a) על /api/newborn/meetings הקיים:
  פילטרים היום/השבוע/הכל (נשמר ב-v2st:meets), באיחור בבלוק אדום למעלה, כרטיסים עם אייקון סוג
  (פגישה=לוח כחול, פולו-אפ=רענון זהב), גלולת שעה, ופעולות: חיוג/וואטסאפ לבעל הנכס, "החתם בעל נכס"
  (prefill v2signPre → /v2/sign?type=owner), ו"בוצע" (confirm → /api/newborn/status/delete — מסיר
  גם מהיומן). FAB + → נכס נולד (שם קובעים פגישה). SWR v2c:meets. "דורש טיפול → הכל" בבית מוביל לכאן.
  תאריכים נתמכים בכל הפורמטים שנמצאו בפועל (ISO/דד-מם, שעה לפני/אחרי).
- 2026-07-06: תוקן "8 בבלעדיות" בבית: הנתון היה summary.exclusives מהדוח = נכסים שגויסו בבלעדיות
  בתקופה (לא "נכסים בבלעדיות") — עכשיו הכיתוב מפורש "N גויסו בבלעדיות השבוע"; למנהל הכרטיס נהיה
  "נכסי המשרד" עם listings (433) במקום "0 פעילים" חסר-משמעות, ו"הצג" מוביל לכל נכסי המשרד.
- 2026-07-06: "הנכסים שלי" ב-/v2/props — הוחזרו בקשות המזכירה מהאפליקציה הישנה (אותם endpoints:
  /api/listing/request kind=price|remove, /api/listing/done): כפתור "עדכן מחיר" (גיליון עם שדה מחיר →
  שליחה למזכירה), אייקון פח 42px על #FBEDED = בקשת הסרת מודעה (confirm), ובנכס שכבר בטיפול — צ'יפ
  מקווקו "בטיפול אצל המזכירה" + כפתור ירוק "הטיפול בוצע". מוצג רק לנכס עם own+id (מספר מודעה).
- 2026-07-06: מפה — סיכת משרד שנייה בזהב: רימקס פמילי ביאליק (אפק), יגאל בשן 2 קרית ביאליק
  [32.85012, 35.10338] (geocode מ-Nominatim); הסיכות תויגו "המשרד · מוצקין" / "המשרד · ביאליק".
- 2026-07-06: מפה — מיקוד חכם (focusDense): במקום fitBounds על כל הנכסים (נכס חריג רחוק היה מרחיק
  את הזום), המפה מתמקדת ב-80% הנכסים הקרובים לחציון (maxZoom 15). נבדק: 12 נכסים בקריות + חריג
  בת"א → זום 15 על הקריות, החריג מחוץ לפריים (עדיין נגיש בגלילה/זום-אאוט).
- 2026-07-06: סרגל הצד (טאבלט/דסקטופ) — נוספו "תהליכים ועסקאות" (/v2/deals) ו"יומן ופולו-אפ"
  (/v2/meets) בכל 10 דפי ה-nav (class dk: display:none במובייל, flex מ-768px). במובייל נשארים 5 טאבים.
- 2026-07-06: מתאמת — תיאום לסוכנים שלה: הצפייה כבר הייתה מסוננת בשרת (/api/newborn/meetings —
  מתאמת=הצוות שלה); נוסף תיאום בשם סוכן: whoami מחזיר למתאמת agent_names (הסוכנים שלה בלבד),
  ובקביעת פגישה/פולו-אפ בנכס נולד נוסף בורר "עבור סוכן" (ברירת מחדל: עליי; מנהל רואה את כל סוכני
  המשרד מ-/api/deals). השרת (api_newborn_status) ממילא אוכף: chosen שלא מהסוכנים שלה — מתעלמים.
  הפגישה נרשמת על הסוכן הנבחר (skey שלו) ונשמרת ביומן שלו.
- 2026-07-06: באג "פולו-אפ בבית אבל לא בפגישות": הגיליון בנכס נולד קרא rs[1].meetings אבל
  /api/newborn/meetings מחזיר results — הרשימה שם הייתה תמיד ריקה (אומת בפרוד: שני המקורות מחזירים
  את אותן 4 רשומות). תוקן + צ'יפ "פגישות ופולו-אפ" בנכס נולד מוביל עכשיו למסך המלא /v2/meets (21a)
  במקום לגיליון המקוצר.
- 2026-07-06: עסקאות — **עמלה סוכן 2**: כשנבחר סוכן 2 בטופס סגירת עסקה מופיע שדה "עמלה סוכן 2"
  (חישוב אוטומטי 2%+מע"מ כמו סוכן 1, עיפרון לדריסה ידנית — commission2/commission2_manual ב-deals.json;
  בלי סוכן 2 השדה מוסתר ונשמר ריק). בכרטיס העסקה: "עמלה סוכן 1: X · סוכן 2: Y".
- 2026-07-06: נכס נולד — "צפייה במודעה" מוצג גם כשאין שם/טלפון של בעל הנכס (הקישור היה כלוא בתוך
  בלוק הבעלים שמוסתר כשאין פרטים; עכשיו מודעה עם link בלבד מקבלת שורת oActs עם הקישור).
- 2026-07-06: בורר "עבור סוכן" למתאמת — נוספה אופציית **"אחר במשרד…"**: פותחת את רשימת כל סוכני
  המשרד (מ-/api/deals, נטען פעם אחת). השרת רוכך בהתאם: מתאמת יכולה לתאם גם לסוכן שאינו בצוות שלה,
  כל עוד השם מוכר במשרד (phone-map/config agents) — שם זר עדיין נדחה.
- 2026-07-06: (א) **יומן שימוש — דשבורד זמן באפליקציה**: כרטיס עליון ב-/v2/activity עם בר לכל סוכן
  (זמן היום + מספר פעולות), נגזר מיומן הפעילות: פעולות ברצף עם פער עד 10 דק' = סשן אחד, סשן בודד
  נספר כדקה; ממוין מהפעיל ביותר. (ב) **עדכוני משרד — שליחה בוואטסאפ**: כפתור וואטסאפ ירוק בכל עדכון
  (wa.me/?text= עם "עדכון מהמשרד (שם): גוף") — נפתח בורר אנשי הקשר של וואטסאפ.
- 2026-07-06: תיקוני גיליון ההתאמות (משוב מהאייפון): (1) כפתור X עגול בכותרת — אפשר לסגור בלי לגלול
  25 כרטיסים לתחתית; (2) הכותרת מציגה "מחפש: <השאילתה>", וכשלקונה אין "מה מחפש" — אזהרה בזהב
  "ההתאמה לפי תקציב בלבד"; (3) matchQuery משתמש בסיכום השיחה כשאין search (התקציב מצטרף תמיד);
  (4) כשיש דרישות אמיתיות מסוננות התאמות מתחת ל-60% (אם אין חזקות — מוצגות 5 המובילות בלבד).
  שורש ה"תוצאות לא קשורות": קונה עם תקציב בלבד → כל מה שבתקציב חוזר ב-30%.
- 2026-07-06: רפי פעמיים בבורר "עבור סוכן" — אותו סוכן בשני איותים (דה פיצ'וטו/דהפיצוטו) משני
  מקורות. הפתרון של הגרסה הישנה (דדופ קנוני, עדיפות לאיות מאנשי הקשר) הוחל גם כאן: (א) ענף המתאמת
  ב-/api/my/agents עבר לדדופ by _canon_key (אומת: שני האיותים מתמזגים); (ב) בורר הפגישות ב-v2 עבר
  מ-whoami.agent_names (הוסר) ל-/api/my/agents, ו"אחר במשרד…" ל-/api/agents (נפתח גם למתאמת);
  (ג) גם המנהל מקבל עכשיו את הרשימה הקנונית במקום שמות גולמיים מגיליון השיחות.
- 2026-07-06: נכסים "שלי" — מתאמת/מנהל/צוות רואים את **שם הסוכן** של כל נכס במקום "שלך"
  (MINE_MULTI מדגל multi של /api/my/properties, גם בקאש); לסוכן בודד נשאר "שלך".
- 2026-07-06: תהליכים ועסקאות — נוסף "בס\"ד" קטן ומעודן בפינה הימנית העליונה של הכותרת (בקשת אייל).
- 2026-07-06: תפריט הצד — "הזמנה לאפליקציה בוואטסאפ": פותח וואטסאפ עם הודעת הזמנה (שם המשרד
  white-label מ-document.title + location.origin/v2) — בוחרים איש קשר ושולחים.
- 2026-07-06: טופס החתמת בעל נכס — ברירת מחדל: **מכירה + בלעדיות** מסומנות, תקופת בלעדיות
  **6 חודשים מהיום** (תאריכים ממולאים אוטומטית), ונוספו צ'יפי תקופה 3/6/12 ח׳ שממלאים מ/עד בלחיצה
  (עריכת תאריך ידנית מנקה את בחירת הצ'יפ). המיפוי לחוזים ללא שינוי: בלעדיות מסומנת → OWNER_EXCLUSIVE
  (כולל עמלת המכירה).
- 2026-07-06: "רפי לא קיבל SMS אחרי חתימה" — שני שורשים ותוקנו שניהם: (1) לא היה בכלל SMS לסוכן
  על חתימה (רק פוש + וואטסאפ, והוואטסאפ האוטומטי מושהה לבקשת אייל) — נוסף _sms_agent_signing:
  SMS לנייד האישי (לא לוירטואלי) עם לקוח/כתובת/קישור למסמך, בשני מסלולי ה"נחתם" (במקום + מרחוק),
  לא תלוי במתג wa_auto; (2) _phones_for_name עבר להתאמה קנונית גם מול מפות הטלפונים — שני האיותים
  של רפי (דה פיצ'וטו/דהפיצוטו) נפתרים לאותם טלפונים (מתקן גם פוש/וואטסאפ/התראות שיחה לפי שם).
- 2026-07-06: השהיית נכס נולד לא תפסה לרפי — אותו שורש איות: המפה נבנית לפי _norm_name של השם
  בקונפיג, השליפה לפי שם הסשן (איות אחר) → נפל לברירת המחדל. תוקן: _fetch_newborn_delays שומרת
  גם מפתח קנוני ("c:"+_canon_key) לכל שם/כינוי (גיליון+קונפיג), והצרכן נופל אליו לפני ברירת המחדל
  (כולל בדיקת "יש הגדרה אישית" מול מנהל מושהה). אומת ביחידה: השהיה 0 באיות אחד נשלפת נכון באיות השני.
- 2026-07-06: ניהול — כרטיס **"שמות לא משויכים · מניעת כפילויות"** (כמו באפליקציה הישנה): מציג שמות
  מחתימות/נכסים שלא מזוהים לאף חבר צוות (unmatchedSignings/Listings שכבר חוזרים מ-/api/dev/people),
  עם בורר סוכן + "שייך" → /api/dev/alias (נשמר ככינוי בקונפיג, מאחד חתימות/השהיות/התראות מיד).
  מוצג רק כשיש לא-משויכים; מוסתר כשהרשימה ריקה.
- 2026-07-06: סרגל צד דסקטופ — סודר תג נכס נולד: order:9 + margin-inline-start:auto (נדחף לקצה
  השורה), align-self:center, פונט/פדינג מוקטנים — השורה מיושרת כמו שאר הפריטים.
- 2026-07-06: יומן ופולו-אפ — כפתור העיפרון היה "החתם בעל נכס" (מבלבל — נראה כעריכה) והוחלף
  ב**עריכת הפגישה/פולו-אפ**: גיליון עם מתג פגישה/פולו-אפ, מועד (datetime-local ממולא מהקיים) והערה —
  שמירה דרך /api/newborn/status/edit הקיים (מעדכן גם את אירוע היומן). ההחתמה נגישה כרגיל ממסך
  החתימות/ההתאמות. נוספו ל-V2_MEETS גם sheet/ovl (לא היו בדף).
- 2026-07-06: קביעת פגישה/פולו-אפ בנכס נולד — נוסף שדה **הערה במלל חופשי** בגיליון הקביעה (נשמר
  ברשומה — api_newborn_status שומר עכשיו note גם ביצירה, לא רק בעריכה); ההערה מוצגת במסך היומן
  וניתנת לעריכה שם.
- 2026-07-06: "הוספתי קונה חם לליקה ולא רואה אותו" — הקונה כן נשמר בשרת (אומת בפרוד: גיל · ליקה,
  שורה 56) אבל דגל החם לא נשמר והפילטר השמור ("חמים") הסתיר אותו. תוקן בטופס הוספת קונה:
  (א) בורר "שייך לסוכן" למנהל/מתאמת (/api/my/agents הקנוני; ה-as כבר קיים ב-/api/buyers/add ונאכף);
  (ב) מתג "קונה חם" — אחרי השמירה הקונה מאותר ברענון (טלפון/שם) ומסומן hot; (ג) אחרי הוספה הפילטר
  חוזר ל"פעילים" כדי שהקונה החדש ייראה מיד. סימון חם ידני לגיל (שורה 56) בוצע כבר בפרוד.
- 2026-07-06: "חתימה שנשלחה לא מופיעה מיד בממתין לחתימה" — הכתיבה הולכת לגיליון (Apps Script)
  אבל הקריאה מ-Supabase (SIGNATURES_SOURCE), והסנכרון ביניהם לוקח דקות; ניקוי הקאש לא עוזר כי המקור
  עצמו מפגר. תוקן בגשר נראות: _RECENT_SIGNS בזיכרון (15 דק') — send_remote/submit רושמים את הרשומה,
  get_signings ממזג אותה אם עוד לא הגיעה מהמקור (דדופ event_id+deal_type — כשהסנכרון נוחת אין כפילות).
  אומת ביחידה: טרייה מופיעה מיד, אחרי "נחיתת הסנכרון" אין כפילות.
- 2026-07-06: "חתימה שנמחקה יורדת מהלוח באיחור" — הצד השני של פער הסנכרון: המחיקה נכתבת לגיליון
  אבל Supabase מחזיק את השורה עד הסנכרון. נוסף גשר מחיקות סימטרי לגשר החתימות הטריות:
  _RECENT_SIGN_DELS (15 דק') — api_sign_delete רושם (eid, received, client) ו-get_signings מסנן
  התאמות עד שהמקור מתעדכן. אומת ביחידה: מחיקה לפי eid+received מורידה רק את השורה המדויקת;
  לפי לקוח+זמן כשאין eid.
- 2026-07-06: **דף "חיבור משרד חדש"** /v2/onboard (מפתח בלבד, קישור מכרטיס הגדרות המשרד בניהול):
  צ'קליסט כל המקורות להרמת משרד מאפס — 8 קטגוריות (זהות/טלפוניה/וואטסאפ/נתונים/Google/התראות/
  צוות/תשתית, 19 פריטים) עם נקודת סטטוס חיה למשרד הנוכחי (endpoint /v2/api/onboard — בוליאנים
  בלבד, אף סוד לא נחשף) + "סדר הפעולות המומלץ" (8 שלבים). הסטטוסים נגזרים מ-env/config:
  שם/לוגו/office_id/קישורים, vphone/SMS, Maytapi/קבוצות, Supabase/AppsScript/SheetsAPI/חוזים,
  OAuth, OneSignal, מנהלים/סוכנים/מתאמות (ספירות), SESSION_SECRET/APP_BASE_URL.
- 2026-07-06: ניהול — (א) **"הצג את כל N חברי הצוות"**: כפתור שפותח את הרשימה המלאה (במקום 5+חיפוש
  בלבד), עם "הצג פחות" לקיפול; (ב) **מחיקת שם לא-משויך**: פח ליד "שייך" בכרטיס הכפילויות —
  agent_delete מוסיף ל-removedAgents, ו-_scan ב-/api/dev/people מסנן עכשיו גם removedAgents (שם
  שנמחק נעלם גם מרשימת הלא-משויכים); (ג) נחקר "מחקתי סוכנים והם חזרו": removedAgents בפרוד היה ריק
  — המחיקות לא נשמרו כלל (כנראה טוקן שפג באותו רגע; המנגנון עצמו אומת חי בפרוד ועובד). למחוק שוב.
- 2026-07-06: "כמעט כולם מכניסים קונה פעמיים" — נוספה חסימת כפילות **בשרת** (/api/buyers/add):
  טלפון שכבר קיים בקונים (או אותו שם אצל אותו סוכן כשאין טלפון) נדחה עם "הקונה כבר קיים אצל X"
  ו-dup:true; מגן על כל המסלולים כולל האפליקציה הישנה (שם יוצג בהודעת הכישלון). ב-v2 (שיחות+קונים):
  confirm "להוסיף בכל זאת?" → שליחה חוזרת עם force:true למקרים מכוונים.
- 2026-07-06: שינויים בבריף הבוקר: (1) המלל בריבועים הוגדל (מספר 24→30px, תווית 10.5→13px מודגשת);
  (2) הקשה על אמצע המסך (שליש אמצעי) משהה/ממשיכה את הסטורי — הפס קופא וממשיך מאותה נקודה, הרמז
  בתחתית מתעדכן ("מושהה — הקש באמצע להמשך"); הקשות בצדדים נשארו הבא/הקודם; (3) ריבועי הדף הראשון:
  קונים שלי / חתימות קונים / בלעדיות — מספר הסוכן גדול ולמטה "המשרד · N" (endpoint חדש /v2/api/brief,
  7 ימים, השוואה קנונית לשיוך "שלי").
- 2026-07-06: בריף — הכרטיס השני הוחלף מ"קונים חדשים" ל**השוואה תחרותית של שיחות**: "שיחות שלך
  השבוע" (מספר גדול) מול "המשרד כולו ענה ל-N — אתה X% מהפעילות"; CTA "לשיחות שלי". /v2/api/brief
  הורחב ב-callsMe/callsAll (7 ימים, שיוך לפי שם קנוני או טלפון וירטואלי).
- 2026-07-07: יומן ופולו-אפ — כפתור ה-+ פותח **יצירה ידנית** (במקום ניווט לנכס נולד): מתג
  פגישה/פולו-אפ, כתובת/נושא (חובה), שם+טלפון אופציונליים, "עבור סוכן" (מתאמת/מנהל), מועד, הערה —
  נשמר דרך /api/newborn/status עם key סינתטי manual:<ts> (נכנס לרשימה, ליומן Google, ניתן לעריכה
  ולסימון בוצע כרגיל). בנוסף: api_newborn_status מחזיר עכשיו save_failed כששמירת הקונפיג נכשלת
  (עד עכשיו דיווח הצלחה כוזבת).
- 2026-07-07: "בבריף 6 משימות אבל אצל אוצר רואים 1" — שוחזר בפרוד עם loginas: אוצר בן חמו הוא
  מתאם/מנהל צוות, ו-/api/report למתאמת בלי as חוזר בסקופ "כל המשרד" (6 פגישות של כולם), בעוד
  /api/newborn/meetings מסנן נכון לצוות (1). תוקן במקור: הבית (בריף כרטיס 2 + "דורש טיפול") ניזון
  עכשיו מ-/api/newborn/meetings — אותו סינון בדיוק של מסך היומן לכל תפקיד; rep.meetings נשאר fallback.
- 2026-07-07: עוז דנילוב "חזר להימחק" אחרי שחזור — נמצא וקטור lost-update ב-_save_config: אחרי
  אתחול שרת (דפלוי) _LAST_GOOD_CONFIG ריק → השמירה הראשונה (למשל gauth מכניסת Google, שלא מתועדת
  ביומן) כותבת את *כל* המפתחות מתוך הטעינה שבידה — אם הייתה ישנה, דורסת שינויים טריים. תוקן:
  כש-prev חסר משווים מול fetch_config טרי מ-Supabase וכותבים רק מה שהשתנה באמת. עוז שוחזר שוב
  בפרוד (אומת אחרי הדפלוי); התיקון בקוד מגן על הדפלויים הבאים.
- 2026-07-07: (א) **מתאמת לא רואה תהליכים/עסקאות של הסוכנים שלה** (בקשת אייל): _deals_can_see —
  מתאמת ירדה מהעדפת-על, רואה רק את שלה (+צוות שיתוף אם היא בו); גם "צפה כסוכן" ב-/api/deals הוגבל
  למנהל. (ב) הרשאות עריכה/מחיקה של פגישות כבר תואמות (סוכן=שלו, מתאמת=צוותה, מנהל=הכל). (ג) בכרטיסי
  שת"פ בהתאמות נוסף "צפייה במודעה בנדל\"ן וואן" (p.link מ-/api/search/exclusives). (ד) המשך תעלומת
  עוז: הוא נמחק שוב גם בלי דפלוי — parity חשף שבלוב הגיליון נשאר ישן ב-removedAgents/agents ויש
  סנכרון גיליון→Supabase שמחזיר את הישן. תוקן ע"י יישור שני הצדדים (dev/config מלא) — parity ✅;
  גיבוי הבלוב ב-_save_config מפסיק להיבלע בשקט (log.error על כשל).
- 2026-07-07: טופס פגישה חדשה נחתך באייפון (הכל מוסט שמאלה) — גלישה רוחבית: ל-input[type=datetime-local]
  ב-iOS יש רוחב מינימלי משלו + שורת שם/טלפון בלי min-width:0, ו-iOS מזיז (pan) את כל התצוגה כולל
  fixed. תוקן ב-V2_MEETS: overflow-x:hidden על html/body/#sheet, min-width:0 ל-fld2 ולשדות,
  -webkit-appearance:none ל-datetime-local. אומת: אפס גלישה, הגיליון 375px בדיוק.
- 2026-07-07: עוז שרד 5 דקות אחרי יישור שני הצדדים — אישוש שהשורש היה בלוב הגיליון הישן.
- 2026-07-07: טופס פגישה חדשה — (א) נעילת רקע מלאה בפתיחת גיליון: overflow hidden גם על html
  (לא רק body/main) + overscroll-behavior:contain על הגיליון + touch-action:none על השכבה —
  הרקע לא זז יותר ב-iOS; (ב) כפתור X עגול בכותרת של "פגישה/פולו-אפ חדש" ושל גיליון העריכה —
  סגירה בלי לגלול לתחתית.
- 2026-07-07: **חבילת תיקוני התאמות/קונים/בית/דוחות** (10 בקשות אייל):
  (1) התאמות — בחירה מרובה: כפתור וי בכל כרטיס + פס "שלח N נכסים לקונה בוואטסאפ" (הודעה ממוספרת
  אחת); שליחת נכס בודד נשארה. (2) ההודעה ללקוח בלי שם הסוכן/המשרד המקורי (msgLine). (3) אין יותר
  קיצוץ ל-5: חזקות (60%+) ואז "התאמות נוספות" — הכל מוצג. (4) דדופ שת"פ מול משרד (אותה כתובת+מחיר
  בהפרש ≤100 אלף — נשאר של המשרד) בהתאמות ובדף הנכסים. (5) פסיקים אוטומטיים בתקציב בהוספת קונה
  (שני הטפסים). (6) עריכת טלפון הקונה בגיליון העריכה (+phone ב-/api/buyers/update — לוודא אחרי
  דפלוי שה-Apps Script updatebuyer כותב את השדה!). (7) "קונים מתאימים" בנכסים — מהקונים השמורים
  בלבד (ניקוד: תקציב/אזור/חדרים) + הכפתור נוסף גם לשת"פ (עם "למודעה"). (8) בית — "הנכסים שלי" קוצר
  לחצי + כפתור "יומן" לצידו (→ /v2/meets, עם מונה). (9) אזור אישי בלחיצה על האווטאר: תמונת פרופיל
  (העלאה→144px→/v2/api/avatar, נשמר בדיסק v2_avatars/<last9>.jpg) + סנכרון יומן Google גם אחרי
  כניסת SMS; התמונה מוצגת בעיגול שבכותרת. (10) דוחות — סוכן רואה דירוג ("מקום X מתוך Y",
  summary.myRank מהשרת — מחושב על כל המשרד לפני הסינון) ורשימת מובילים בלי המספרים; מנהל ללא שינוי.
  בדיקת נתונים בפרוד: חתימות 31=31, נענו 34=34, נכסים 433 ✓; פער שיחה 1 = שיחה מוסתרת (תקין).
- 2026-07-07: (א) אופיר שמול לא הצליחה להתחבר — הייתה בקונפיג **בלי טלפון** (רק שם+newbornDelay,
  כנראה קורבן נוסף של בלוב-הקונפיג הישן או הוספה חלקית); הוצמד לה 0559732278 + role=agent ישירות
  בפרוד ואומת ש-auth/request מזהה (נשלח לה קוד). (ב) נכס נולד — כפתור X בתיבת החיפוש (מופיע כשיש
  טקסט, מנקה ומרנדר; מוצג גם אחרי שחזור חיפוש שמור מ-v2st).
- 2026-07-07: (א) **כל חתימת מתעניין → "קונים שלי" אוטומטית, מכל מקור**: לולאת רקע (_sync_signing_buyers,
  כל 10 דק') סורקת חתימות קונים מ-14 הימים האחרונים (label "קונים"/מתעניין — תופס גם שורות שיטס)
  ומוסיפה קונה חסר דרך _add_buyer_from_signing (דדופ מובנה). (ב) **שכבת מהירות V2_BOOST** בכל הדפים:
  whoami עונה מיידית ממטמון מקומי (10 דק', רענון ברקע; !ok → ניקוי+מסך כניסה; כל החלפת/ניקוי fbTok
  מוחקת v2who), preconnect לפונטים, prefetch דף הטאב בנגיעה/ריחוף (דפי v2 עברו ל-max-age=30 כדי
  שה-prefetch ייתפס), והלוגו public,max-age=3600 (היה no-store — ירד בכל מעבר טאב).
- 2026-07-07: **באג "קונה חם נעלם אחרי יציאה"** — הסטטוס נשמר כעמודת status על שורת buyers
  ב-Supabase, והסנכרון גיליון→Supabase משכתב את הטבלה ומוחק אותו (אומת: ה-hot מהבוקר נעלם).
  תוקן: הסטטוסים עברו לקונפיג (v2_buyer_status) שהסנכרון לא נוגע בו, במפתח **טלפון הקונה**
  (עמיד גם להזזת שורות במחיקת קונה; בלי טלפון — r<row>). הלקוח קורא לפי טלפון עם נפילה למפתח
  שורה ישן; active מוחק את הרשומה. כל הקוראים עודכנו (toggleHot/עריכה/הוספה-חם).
- 2026-07-07: "עניין עם התוצאות" בחיפוש נכסים — שוחזר: השרת עונה נכון ("שכונת אפקה" → 34 תוצאות
  60% בשכונת אפק), אבל בלקוח היה **מרוץ בקשות**: תשובת חיפוש קודם ("לח\"י 52") שהגיעה באיחור דרסה
  את תוצאות החיפוש החדש (כולל שורת הסיכום). תוקן בשומר רצף (_loadSeq) ב-load של דף הנכסים —
  תשובה שאינה של החיפוש האחרון נזרקת.
- 2026-07-07: פוש נכס נולד — רק למי שהמודעות **פתוחות אצלו מיד** (_nb_push_targets): השהיה 0 מקבל;
  מוסתר, מושהה (N ימים) ומנהל-מושהה לא מקבלים פוש על נכס שעוד לא נחשף להם. מנהלים כן, אלא אם
  הוגדרה להם השהיה אישית. אומת ביחידה: פתוח כן / מוסתר לא / 3 ימים לא.
- 2026-07-07: **הכנה למצגת 100 סוכנים** — בדיקת עומס בפרוד חשפה ש-gunicorn רץ עם worker יחיד
  סינכרוני: 20 בקשות מקבילות = חציון 7.3ש' (תור טורי!). Procfile עודכן ל---threads 12 --worker-class
  gthread (worker אחד, בכוונה — הקאשים/warmer/גשרים בזיכרון משותף; אסור workers>1 בלי הפרדה).
  לוודא אחרי דפלוי בפרץ מקביל חוזר.
- 2026-07-08: הסנכרון דרס גם את מפתח הקונפיג v2_buyer_status (מוחק מפתחות שאינם בבלוב הגיליון) —
  סטטוסי הקונים הועברו ל**קובץ בדיסק הקבוע** (v2_buyer_status.json, כמו deals.json) עם lock —
  חסין לחלוטין לסנכרון. ה-API ללא שינוי חיצוני.
- 2026-07-08: מועדי פגישה/פולו-אפ בקפיצות של 15 דקות (בקשת אייל, ענף bugfix): iOS מתעלם מ-step
  על datetime-local (הגלגל מציג את כל 59 הדקות), לכן שלושת השדות — קביעה בנכס נולד (stDt), יצירה
  ידנית (nmDt) ועריכה (emDt) ביומן — פוצלו לשדה תאריך + בורר שעה נייטיבי (select, רבעי שעה בלבד;
  בגלגל iOS מופיעים רק 00/15/30/45). ברירת מחדל: רבע השעה הקרוב; בעריכה, מועד קיים שאינו על הרשת
  (למשל 10:20) מוזרק לרשימה ונבחר. פורמט הערך לשרת ללא שינוי (YYYY-MM-DDTHH:MM). תיקון הרוחב של
  datetime-local ב-iOS הוחל גם על input[type=date].
- 2026-07-08: גיליון ההתאמות לקונה (בקשות אייל, ענף bugfix): (1) הכותרת ("התאמת נכסים · שם" +
  שורת "מחפש: …" + X) דביקה — נשארת גלויה בגלילת התוצאות (position:sticky בתוך #sheet, רקע הגיליון
  + צל עדין); (2) **סינון תקציב קשיח בלקוח**: מוצגים רק נכסים בהפרש של עד 20% מהתקציב לשני
  הכיוונים (תקציב 2M ⇒ 1.6M–2.4M; נכס בלי מחיר לא נפסל). התקציב נקרא בפרסור סלחני _budgetNum
  ("2,000,000" / "2 מיליון" / "800 אלף" / טווח — לוקחים את הגבוה); תקציב לא מפוענח (<10K) לא מסנן.
  אומת ב-node --check על הדף המוגש + בדיקות יחידה לפרסור ולחלון. הערת אייל: בלי סבבי פריוויו
  בדפדפן — הוא בודק בעצמו באפליקציה; ולידציה סטטית + יחידה בלבד.
- 2026-07-08: (בקשות אייל, ענף bugfix) (א) **הזמנת חבר צוות גם ב-SMS**: בגיליון ההזמנה בניהול
  נוסף כפתור משני "שלח הזמנה ב-SMS" — השרת (/v2/api/admin/invite עם via:"sms") שולח את אותה הודעת
  הזמנה ישירות דרך web_send_sms (ספק ה-SMS של קודי הכניסה); הרישום בקונפיג זהה, וואטסאפ נשאר
  הכפתור הראשי. (ב) **אייקוני כניסה — לוגו אפי** (חריג מכוון ל-white-label, החלטת אייל): favicon +
  apple-touch-icon בכל דפי /v2 (הזרקה מרכזית ב-_page, מסלול /v2/icon-64/180.png מגיש נגזרות של
  docs/handoff/app-icon-1024.png עם cache יום); ואייקון אפליקציית ה-iOS בעטיפת Capacitor
  (remax-family: AppIcon-512@2x.png) הוחלף לאפרוח על נייבי — הושטח ערוץ האלפא (App Store דוחה
  אייקון שקוף). שאר ה-UI נשאר לוגו המשרד. נדרש בילד TestFlight חדש כדי שאייקון האפליקציה יתעדכן.
- 2026-07-08: השלמה אוטומטית של קוד ה-SMS בכניסת v2 (דיווח אייל, ענף bugfix): לשדה הקוד במסך
  הכניסה חסר autocomplete="one-time-code" — iOS לא הציע את הקוד מעל המקלדת. נוסף (ו-autocomplete="tel"
  לשדה הטלפון). האפליקציה הישנה וה-SMS עצמו (שורת ‎@host #code) היו תקינים מראש.
- 2026-07-08: יומן ופולו-אפ — מנהל לא ראה איזה סוכן שייך לפגישות **באיחור** (דיווח אייל): שם הסוכן
  הוצג למנהל/מתאמת רק בשורת המשנה של כרטיסים רגילים; ענף ה"באיחור" השמיט אותו. תוקן — גם בכרטיס
  באיחור: "באיחור · סוג · תאריך · שם הסוכן" (MULTI בלבד; סוכן בודד לא רואה את שמו). אומת ביחידה.
- 2026-07-08: **אי-אפשר לגלול בדסקטופ** במסכי "יומן ופולו-אפ" ו-onboard (דיווח אייל): שני המסכים
  במודל נעילת iOS (`html,body{height:100%}` + `main{overflow:auto}` = main גולל, החלון נעול) — נכון
  למובייל, אבל בדסקטופ אין ניווט תחתון אלא סרגל צד, וגלגלת מעל הסרגל/הכותרת לא הזיזה כלום. תוקן:
  media query ל-≥768px שמחזיר גלילת חלון (`html,body{height:auto};body{min-height:100vh}`) — זהה ל-12
  המסכים האחרים; המובייל ללא שינוי; המפה נשארת height:100vh בכוונה. אומת: דסקטופ 1280 גולל, מובייל 375 נעול.
- 2026-07-08: הזמנת חבר צוות — **SMS כדרך היחידה** (בקשת אייל, במקום הכפתור המשני שנוסף קודם):
  הכפתור הראשי הירוק בגיליון שולח ב-SMS ישירות מהשרת (via:"sms" → web_send_sms), כפתור הוואטסאפ
  הוסר, טקסט העזר עודכן. גם "שלח שוב" (resend) עבר ל-SMS. הבלוק בשרת חל על שני המסלולים (חדש+resend).
- 2026-07-08: **ריצודים באוברסקרול (\"הוסף למסך הבית\"/standalone)** — השורש: הכותרת בזרימה,
  main הוא הגולל הפנימי (overflow:auto) וה-nav position:fixed בלי הפרדת שכבה → iOS מרנדר מחדש את
  הניווט הקבוע בכל פריים גלילה (ריצוד); ול-html אין רקע → פס לבן בבאונס. תוקן ב-V2_BOOST (חל על כל
  דפי הטאבים, ממוקד ב-:has(nav) כדי לא לגעת בכניסה הנייבי): html:has(nav) רקע קרם + overscroll-behavior:none
  על html/body (בלי רבר-band ברמת המסמך), nav מקבל translateZ(0)+backface-visibility:hidden (שכבת GPU,
  בלי ריצוד; נשאר fixed — אומת), ו-main overscroll-behavior-y:contain (הבאונס נשאר בפנים, רקע קרם =
  אוברסקרול שקוף). אומת: html קרם, nav matrix, main contain, ניווט נשאר קבוע בגלילה, כניסה לא הושפעה.
- 2026-07-08: **גלילה נשברה במובייל+אפליקציה אחרי מיזוג תיקון הריצודים** (דיווח אייל): תיקון
  הריצודים (b8d7a61, צ'אט השיפורים) הוסיף `body:has(nav) main{-webkit-overflow-scrolling:touch}`
  **גלובלית** — אבל ב-10 מ-12 הדפים main אינו הגולל (מודל min-height:100vh → **החלון** גולל).
  ב-iOS התכונה יוצרת אזור גלילת-מומנטום על main שאין לו מה לגלול, לוכדת את המגע ומונעת גלילת חלון
  ("אי אפשר לגלול"). ב-Chromium אין את התכונה → לא שוחזר בדסקטופ, רק ב-iOS (ווב+אפליקציה). תוקן:
  הוסרה `-webkit-overflow-scrolling:touch` (מיותרת ב-iOS מודרני — מומנטום הוא ברירת מחדל). שאר תיקון
  הריצודים נשמר (רקע קרם ל-html, overscroll-behavior:none, nav translateZ, overscroll-behavior-y:contain
  ל-main). רגרסיה ב-Chromium: קונים=חלון גולל, מפגשים=main גולל — תקין. **אימות iOS דורש מכשיר.**
  ⚠️ נוגע בבלוק של צ'אט השיפורים (V2_BOOST) — לתאם לפני מיזוג חוזר.
- 2026-07-09: **ריצוד הניווט התחתון במסכי main-scroll** (דיווח אייל: "עדיין מרצד", מסך מסוים, הניווט):
  הסרת -webkit-overflow-scrolling:touch הגלובלית (התיקון הקודם) פתרה את חטיפת המגע ב-10 מסכי
  window-scroll — אבל במסכים שבהם main **כן** הגולל (יומן/מפגשים ו-onboard) היא ביטלה את הגלילה
  המרוכבת ב-GPU, והניווט הקבוע חזר לרצד בגלילה (iOS מרנדר fixed מחדש כל פריים כשהגלילה לא מרוכבת).
  תוקן: -webkit-overflow-scrolling:touch הוחזר **רק ל-2 המסכים האלה** (שם main הוא הגולל היחיד —
  אין חלון להתחרות בו, אז אין חטיפה). 10 מסכי window-scroll נשארים בלי התכונה (מוגנים מחטיפה).
  ⚠️ לא ניתן לשחזר/לאמת ב-Chromium (התכונה iOS-בלבד) — **דורש אימות על אייפון**.
- 2026-07-09: **ריצוד הניווט התחתון — רק ב-PWA standalone (הוסף למסך הבית בספארי)** (דיווח אייל
  + צילום מסך הבית): לא בספארי רגיל, לא באפליקציית Capacitor — רק ב-standalone. שוללתי לולאת רינדור
  (0 מוטציות DOM על nav ב-4ש'). השורש: במסכי window-scroll **המסמך (body) גולל**, וב-iOS standalone
  ניווט position:fixed מרצד בבאונס של גלילת המסמך; translateZ של צ'אט השיפורים לא עזר כי הבעיה
  היא גלילת המסמך עצמה. תוקן: JS מוסיף class `.pwa` ל-<html> **רק** כש-navigator.standalone===true
  (סיגנל iOS-בלבד ל"הוסף למסך הבית"; ספארי רגיל=false, דסקטופ/Capacitor=undefined). ה-CSS ל-.pwa
  נועל את הגוף (height:100%,overflow:hidden) ומעביר את הגלילה ל-main (overflow:auto+momentum) —
  הגוף לא זז, הניווט הקבוע לא מרצד. אומת ב-Chromium: מצב רגיל=חלון גולל בלי הקלאס (אפס רגרסיה);
  בכפיית .pwa=body נעול, main גולל את כל התוכן (טווח 1557px). **הריצוד עצמו iOS-בלבד — דורש אימות
  על אייפון ב-standalone.** נוגע ב-V2_BOOST (בלוק צ'אט השיפורים) — לתאם לפני מיזוג חוזר.
- 2026-07-08: **כניסת גוגל "מחזירה לאפליקציה הישנה" אחרי דפלוי** — השורש: קישורי גוגל↔טלפון
  (gauth) נשמרו בקונפיג, שהסנכרון מהגיליון דרס (אותה משפחה כמו עוז/הקונה החם). אחרי דפלוי הקישורים
  נמחקו → כל סוכן שנכנס עם גוגל נזרק למסך "חיבור ראשון" (styled כמו הישן) במקום כניסה ישירה ל-/v2.
  תוקן: gauth הועבר ל**דיסק הקבוע** (v2_gauth.json) עם lock + שחזור חד-פעמי מהקונפיג (מי שעדיין
  מקושר לא יאבד). _gauth_all/_gauth_link/effie overview עודכנו. אומת: ראונד-טריפ בדיסק, refresh_token
  נשמר, email_for_phone, ו-overview לא נשבר. הכפתור החי כבר שולח next=v2 (dest=/v2/home תקין),
  אז אחרי הדפלוי סוכן מקושר ייכנס ישר ל-/v2 בלי מסך הקישור.
- 2026-07-09: **השורש האמיתי לריצוד ב"הוסף למסך הבית"** (צילומים: בית=מסך מלא, נכס נולד=שורת
  כתובת ספארי מופיעה): **חסרו לגמרי meta של PWA ו-manifest** (apple-mobile-web-app-capable, /manifest.json=404).
  בלעדיהם "הוסף למסך הבית" ב-iOS יוצר **web-clip של ספארי** עם שורת כתובת שמתקפלת בגלילה = "מגדיל
  את המסך" + ריצוד הניווט הקבוע, חמור מכל בנכס נולד (הרשימה הכי ארוכה). זה גם הסביר למה תיקון ה-.pwa
  הקודם לא עזר — הותנה ב-navigator.standalone===true, שאף פעם לא היה true בלי ה-meta. תוקן: הוזרקו
  ל-_V2_ICON_TAGS (כל דפי /v2) apple-mobile-web-app-capable=yes + mobile-web-app-capable +
  status-bar-style=default → פתיחה במסך מלא בלי שורת כתובת → אין קיפול, אין ריצוד; וגם מפעיל את
  תיקון ה-.pwa כבונוס. **דורש הוספה-מחדש למסך הבית אחרי דפלוי** (ה-meta נקרא ברגע ההוספה).
- 2026-07-08: **חבילת תיקונים (עסקאות/תהליכים/דוחות)** [main; הרינדור בצ'אט הבאגים במקביל]:
  (1) עסקאות ותהליכים — סוכן 2 עבר מ-select ל**מלל חופשי עם השלמה אוטו** (datalist agentsDL); חל
  על שני הטפסים. (2) עסקה חדשה — תאריך סגירה **לא חובה** (ריק → היום ברגע השמירה, בשרת);
  מחיר המכירה מוצג ברירת-מחדל **מהתהליך** (it.sale_price || it.price). (3) תהליך — checkbox
  "הוגשה הצעה על הנכס" (offer) בשורת הסוכנים. (4) עו"ד — פוצל ל**עו"ד קונה + עו"ד מוכר** (fLaw/fLaw2,
  lawyers/lawyers2 בשרת). (5) דוחות מנהל — נוסף צ'יפ **"היום"** (period=day בשרת) + כרטיס **סיכום
  פגישות ופולו-אפ** (רשימה מפורטת עם ספירת פגישות/פולו-אפ). כל השדות החדשים נשמרים ב-deals.json.
  הערה: השלמת SMS אוטומטית — צד הווב כבר תקין (autocomplete=one-time-code + פורמט @domain #code
  בשרת); אם לא עובד בפועל צריך לבדוק את תוכן ה-SMS שמגיע מהספק (sms.deals).
- 2026-07-08: תהליך — סימון "הוגשה הצעה על הנכס" חושף עכשיו שני שדות: **הצעת מוכר + הצעת קונה**
  (offer_seller/offer_buyer, נשמרים ב-deals.json). פסיקים אוטומטיים (nfmt/nfix) בכל שדות המחיר:
  מחיר (תהליך), מחיר מבוקש + מחיר מכירה (עסקה), ושתי ההצעות. אומת חי.
- 2026-07-08: **שעות שקט 22:00–08:00** (שעון ישראל, _quiet_hours) — חוסם **פוש** (send_push) ו**וואטסאפ
  אוטומטי** (_wa_auto_on מחזיר False בלילה). לא חל על: SMS קוד כניסה (web_send_sms — חייב לעבוד תמיד),
  תשובות בוט לפקודות, ושליחת חוזה ע"י סוכן (פעולות יזומות). ניתן לכוונון ב-env QUIET_HOURS_START/END.
  הערה: SMS לסוכן על חתימה (web_send_sms) לא נחסם — אם רוצים גם אותו בשקט, לומר. אומת: 22/23/00/03/07
  חסומים, 08/09/15/21 פעילים; zoneinfo פעיל.
- 2026-07-08: **גלילה תקועה בדסקטופ** — צ'אט הרינדור שינה את V2_BOOST כך שהחלון הוא הגולל (body גדל),
  אבל main נשאר overflow:auto + overscroll-behavior:contain. בדסקטופ העכבר מעל main → הגלגלת מנסה
  לגלול את main (שאין בו מה לגלול) וה-contain חוסם גלישה לחלון → תקוע. תוקן ב-V2_DESKTOP_CSS (ממוקד
  ≥768px, לא נוגע במובייל/PWA של הצ'אט השני): body:has(nav) main{overflow:visible;overscroll-behavior:auto}
  — main אינו מיכל-גלילה בדסקטופ, הגלגלת גולשת לחלון. אומת: 1400px גלילה ל-500 עובדת, מובייל ללא שינוי.
- 2026-07-08: **השבתת v1 — מעבר מלא לאפי מחר בבוקר** (בקשת אייל): /app מפנה 302 ל-/v2 (V1_REDIRECT,
  ברירת מחדל ON). הטוקן ב-localStorage משותף לאותו origin — סוכן מחובר נכנס ישר; לא מחובר מקבל את
  מסך הכניסה של /v2. **לא נגע ב-/api/** (אפי תלוי בו) ולא בעטיפת ה-Capacitor (server.url=/v2 ממילא).
  **שסתום חירום:** V1_REDIRECT=0 ב-Render env מחזיר את v1 מיידית בלי דפלוי קוד. הערה: דיפ-לינק
  \"הוסף קונה\" הישן (/l/<tok> → /app#ab=) לא נתמך אחרי ההפניה — דורמנט ממילא (wa_auto כבוי).
- 2026-07-08: **זיכרון גלילה מדויק בנכס נולד תוקן** — נשבר כי צ'אט הרינדור העביר את הגלילה מ-main
  לחלון, אבל הקוד קרא/כתב main.scrollTop (תמיד 0) והאזין לגלילת main (לא קורית). שוכתב אגנוסטי-לגולל:
  _scrY (max של pageYOffset/scrollingElement/main), _scrTo (מגדיר את שלושתם), שמירה על window+main
  scroll + pagehide + visibilitychange. שחזור **סינכרוני** ב-render (הגדרת scroll מאלצת layout;
  ה-rAF תקע את הפריוויו וגם פחות אמין), בתוך חלון של 2.5ש' מהטעינה — שורד את ה-render של load ולא
  קופץ בשינוי פילטר; גלילת משתמש (מחוץ לחלון _restoring 80ms) עוצרת ומנקה. אומת חי: שחזור ל-950
  מדויק, שורד render-חוזר, גלילת משתמש משתלטת בלי snap-back.
- 2026-07-09: **נכס נולד — 0 ימים לכולם, ביטול מלא של ההשהיות** (בקשת אייל): נוסף דגל
  `NEWBORN_DELAYS_DISABLED` (ברירת מחדל **1=מבוטל**). כשמבוטל: `_fetch_newborn_delays()` מחזיר
  `{"_default":0}` (מתעלם מגיליון ההשהיות ומקונפיג `newbornDefaultDelay`, בלי מוסתר), ו-`_delayed_admin_days`
  מחזיר 0 (גם אווה אזולאי / מנהל מושהה רואה מיד). התוצאה: כל סוכן delay=0, כולם רואים את כל המודעות
  מיד, ופוש נכס נולד יוצא לכולם. אומת ביחידה: שתי הפונקציות מחזירות 0. הגדרות אישיות שמורות בקונפיג
  לא נמחקו אך **לא נאכפות** (מוצגות בקונסולה, מתעלמים מהן). **החזרה:** `NEWBORN_DELAYS_DISABLED=0`
  ב-Render env — בלי שינוי קוד. הערה: הדגל עוקף גם את בטיחות ה"כישלון→מוסתר" (מכוון — אין יותר הסתרה).
- 2026-07-09: **עסקאות — סנכרון חי מקובץ המעקב ב-Dropbox** (בקשת אייל): קובץ אקסל מוצפן (סיסמה)
  שהמזכירה מעדכנת. נוסף ל-app.py: `_fetch_dropbox_deals` מוריד את הקובץ (המרת קישור שיתוף ל-dl=1),
  מפענח עם msoffcrypto, קורא גיליון "עסקאות 26" (זיהוי עמודות **לפי שם כותרת** — עמיד לשינוי סדר),
  ממפה שם-קצר→שם-מלא דרך `_DROPBOX_AGENT_MAP` (מפורש; צוותים=כמה שמות: אלי+יהודית שמול, ליאור
  גרונפינקל+מאור קבלי; "הרשקו וקובי"→מתן הרשקו בלבד), מסנן **2026 בלבד**. ב-`/api/deals/import`:
  מקור אמת = הקובץ החי אם זמין, אחרת `sales_seed_2026.json` (עודכן ל-210 רשומות כתצלום/fallback).
  requirements: +openpyxl +msoffcrypto-tool. **env ב-Render:** `DEALS_XLSX_URLS` (קישורי Dropbox,
  מופרד בפסיקים לריבוי קבצים), `DEALS_XLSX_PWD`. בלי env → נופל ל-seed (210). אומת חי מקצה-לקצה:
  210 עסקאות, 18 צוות, אפס שם לא-ממופה. **קובץ שני** של המזכירה — יתווסף ל-DEALS_XLSX_URLS (אחרי
  בדיקת מבנה). הסיסמה/קישור לא בקוד — רק ב-env.
- 2026-07-08: **גלילה תקועה בנכס נולד באנדרואיד** — אותו מלכוד כמו הדסקטופ, במגע: .pwa מזוהה רק
  ב-iOS (navigator.standalone), אז אנדרואיד במסלול חלון-גולל אבל main{overscroll-behavior:contain}
  לוכד את המגע (iOS Safari סלחני ולכן עבד שם). תוקן ב-V2_DESKTOP_CSS (הקוד שלי, בלי לגעת ב-V2_BOOST
  של צ'אט הרינדור): html:not(.pwa) body:has(nav) main{overflow:visible;overscroll-behavior:auto} —
  חל על כל הרוחבים ללא-PWA (אנדרואיד/ספארי/דסקטופ); גובר על V2_BOOST בספציפיות. iOS-PWA (html.pwa)
  שומר main-scroll+contain (תיקון הריצוד). אומת: לא-PWA main=visible/auto, PWA main=auto/contain.
- 2026-07-09: **עסקאות — קובץ Dropbox שני** (סניף/קבוצה נוספת, בקשת אייל): מבנה שונה מעט —
  גיליון "עסקאות26" (בלי רווח) וסדר עמודות אחר; טופל ע"י **זיהוי גיליון אוטומטי לפי כותרות** (מחפש
  גיליון עם "סוכן"+"סכום העסקה") + זיהוי עמודות לפי שם (כבר היה). **סיסמה שונה** (5175 מול 1324) —
  התמיכה עברה ל-`DEALS_XLSX_PWDS` (מקבילי לפסיקים; גיבוי ל-DEALS_XLSX_PWD היחיד). שמות קובץ 2 רובם
  כבר מלאים (זהות); שים לב ל**שתי אירנות** (אירנה אוברוב מקובץ 1, אירנה טנדשוילי מקובץ 2 — נבדל כי
  מלא). "יהונתן"=סוכן לשעבר (זהות), "שי ושרלי"=צוות [שי, שירלי גוטמן]. סה"כ שני הקבצים: **358 עסקאות 2026**,
  44 סוכנים. seed עודכן ל-358 (fallback). **env:** DEALS_XLSX_URLS=url1,url2 · DEALS_XLSX_PWDS=1324,5175.
- 2026-07-09: השלמת מיפוי קובץ 2: "יהונתן"→זהות (סוכן לשעבר), "שי ושרלי"→צוות [שי, שירלי גוטמן].
  שני הקבצים סגורים: 358 עסקאות. הערה: קובץ 2 כולל גם שכירויות (side="שוכר", סכומים חודשיים) —
  נכנסות כעסקאות רגילות.
- 2026-07-09: **מנהל קבוצה — מתאמת שרואה גם תהליכים/עסקאות** (בקשת אייל, לאוהד פלד): החלטת
  07/07 (מתאמת לא רואה עסקאות) נשמרת לכל המתאמות; חריג פר-מתאמת דרך קונפיג `v2_deals_group_managers`
  (שמות/טלפונים). ב-_deals_can_see: מתאמת שברשימה מוסיפה את agent_names שלה ל-keys → רואה
  עסקאות/תהליכים של הסוכנים שלה בלבד (לא כל המשרד). שיחות/קונים/חתימות/נכס-נולד כבר עבדו למתאמת
  (scope). אומת ביחידה: רגילה=False, מנהל-קבוצה=True לסוכן שלו/False לזר. אוהד הוגדר ברשימה בפרוד
  (dev/config). ניהול עתידי: להוסיף/להסיר שמות מ-v2_deals_group_managers (או UI checkbox בהמשך).
- 2026-07-15: **v1 נמחקה סופית** (אישור אייל, אחרי soak של /v2 + /v2/dev): `FAMILY_BOT_HTML` (1,781 שורות) ודגל `V1_REDIRECT` הוסרו מ-app.py. **ראוט `/app` נשאר** כ-redirect קבוע 302→/v2 (אייקוני מסך-בית של סוכנים מצביעים עליו). אפס שינוי ב-/api/ ובנתונים. אומת: ast.parse ✓, יבוא מודול מלא (121 ראוטים) ✓, GET /app→302 /v2 ✓. שחזור אם אי-פעם יידרש: git history (הקומיט שלפני המחיקה).
- 2026-07-15: **קוד ה-SMS לא הוצע אוטומטית בכניסה — שורש + תיקון:** ה-SMS הסתיים ב-`@domain #code` (פורמט domain-bound של אפל). iOS מציע קוד כזה רק בספארי על אותו דומיין או באפליקציה עם Associated Domains — והעטיפה (Capacitor/WKWebView, `App.entitlements` בלי הדומיין) לא כזו → ההצעה מעל המקלדת דוכאה בתוך האפליקציה. תוקן: הוסרה שורת הדומיין משני מסלולי ה-SMS (כניסה + glink) — קוד חופשי מוצע בכל מקום; לא אבד כלום (WebOTP אנדרואיד ממילא לא חווט). בונוס UX בדף הכניסה: הקשה על ההצעה (4 ספרות) → `cdAuto()` נכנס אוטומטית בלי כפתור, עם guard `smsBusy` נגד שליחה כפולה. אומת: ast+import+node. אימות אמיתי — רק על מכשיר אחרי פריסה. אם נרצה בעתיד domain-bound+WebOTP: AASA ב-/.well-known + entitlement `webcredentials` + rebuild של האפליקציה.
- 2026-07-15: **המשך קוד-SMS — הבעיה האמיתית: המפענח של iOS לא זיהה את הנוסח** (אובחן מול המכשיר: הקוד מופיע כטקסט רגיל בהודעות, בלי קו-תחתון/"העתק קוד" — גם בספארי וגם באפליקציה, מקלדת אפל). הסרת שורת ה-@domain (התיקון הקודם) הייתה נכונה אך לא מספיקה. הנוסח "קוד הכניסה שלך ל-Family Bot: 1234 (תקף ל-5 דקות)" נכשל — כנראה הלטינית בין "קוד" למספר + הספרה 5 המתחרה. הוחלף בשני המסלולים לנוסח כפול-דפוס: `קוד הכניסה שלך: 1234` \n `Family Bot code: 1234` \n `(תקף לחמש דקות)`. בדיקת הצלחה אחרי פריסה: הקוד בהודעות מקבל קו-תחתון ומוצע מעל המקלדת. אומת ast+import.
- 2026-07-15: **מיתוג אפי ב-SMS הכניסה (בקשת אייל):** "Family Bot" הוחלף ב"אפי"/"Effie" בהודעת הקוד (שני המסלולים) — חשיפה מודעת נוספת של המותג (אחרי ה-TV), חריגה מכוונת ממצב white-label. הנוסח הסופי: `קוד הכניסה שלך לאפי: X` \n `Effie code: X` \n `(תקף לחמש דקות)`. הערה מהשטח: ספק ה-SMS מוסיף בסוף 'נשלח ע"י: +9725...' — מספר נוסף בגוף ההודעה; אם הזיהוי של iOS עדיין ייכשל אחרי הפריסה, זה החשוד הבא (לבדוק אפשרות להסרה מול sms.deals).
- 2026-07-15: **מסך TV בלי כניסה — מצב קיוסק (בקשת אייל):** `/v2/tv?k=<TV_KEY>` עובד בלי התחברות. מימוש: env `TV_KEY` ב-Render (ריק=כבוי, הכל כמו קודם); `_tv_key_ok()` (compare_digest) ב-register; `/v2/api/brief` מקבל `?k=` כאימות חלופי → סשן צופה כלל-משרדי לקריאה בלבד (name ריק — מוני "שלי" לא רלוונטיים ל-TV); ה-JS של ה-TV קורא `k` מה-URL ומעביר אותו ל-GET (בלי טוקן). כניסה רגילה (עם fbTok) ממשיכה לעבוד במקביל. אומת: ast+node+integration (בלי env: 401; עם env: מפתח נכון 200+דאטה, שגוי/חסר 401). שימוש: להוסיף TV_KEY ב-Render env → לפתוח בטלוויזיה את הקישור עם ?k= פעם אחת.
- 2026-07-15: **TV_KEY בפרוד = מפתח קצר (החלטת אייל, מודעת):** אייל בחר מפתח 6 ספרות ל-/v2/tv ואישר במפורש שחשיפת נתוני הבריף אינה מדאיגה אותו. לא להציע שוב חיזוק — ההחלטה מתועדת.
- 2026-07-15: **הכנות App Store (בקשת אייל; שם בחנות: אפי):** (1) `/privacy` — דף מדיניות פרטיות סטטי ציבורי ב-app.py (דרישת אפל; עברית, פלטת אפי, מייל פנייה). (2) **חשבון ביקורת לאפל:** `_BYPASS_LOGINS={"501234567":"1905"}` — כניסה 0501234567 עם קוד קבוע 1905 בלי SMS; **תיקון אבטחה נלווה:** בייפאס לא-רשום מקבל עכשיו role=agent (היה admin — מסוכן לבודק על פרודקשן; DEV_PHONES עדיין מקבלים admin דרך _is_dev). להסרה אחרי אישור החנות. (3) CFBundleDisplayName ב-remax-family: "Family Bot"→"אפי" (דורש build חדש, 12). אומת: ast+integration (privacy 200; verify קוד נכון→agent, שגוי→נדחה).
- 2026-07-15: **/privacy שודרג לדף המעוצב מחבילת ה-handoff** (אייל הצביע על privacy-standalone.html מ-08/07 — חולץ מקובץ ה-bundle): 9 סעיפים מלאים (ח.פ פמילי נדל"ן והשקעות, הקלטת שיחות, AI, Google API, RLS, מחיקת חשבון, פרטי קשר). מולאו החוסרים: ספק AI=Anthropic (Claude); טלפוניה/תמלול=ניסוח גנרי (הספק המדויק לא אומת — לא הומצא); תקופת שמירת הקלטות=ניסוח גנרי; באנר "טיוטה לעו"ד" הוסר; **סעיף מחיקת החשבון נוסח לפי המציאות** (פנייה למנהל/מייל, 30 יום) — במקור הבטיח מסך "הגדרות←מחיקת חשבון" שלא קיים. פונטים: Google Fonts במקום קבצי ה-bundle. אומת חזותית + integration. ⚠️ מומלץ עדיין להעביר עו"ד לפני/אחרי ההגשה.
- 2026-07-15: **צילומי מסך ל-App Store (נוצרו):** 8 צילומים ב-1320×2868 (6.9" — הגודל שאפל דורשת) נשמרו ב-`~/Downloads/effie-appstore-screenshots`. שיטה: Playwright (chrome-headless-shell 1217 מהמטמון) על האתר החי /v2, viewport 440×956 @ DPR3, התחברות דרך חשבון הביקורת (0501234567/1905) בהזרקת טוקן ל-localStorage + חימום /v2/api/brief. ⚠️ computer-use על הסימולטור נכשל (הקליקים דיווחו "com.apple.dock" — באג מיפוי מול Retina+Stage Manager; כיבוי Stage Manager לא עזר, שוחזר). לכן Playwright, לא הסימולטור. המסכים: 1_login, 2_home (דשבורד), 3_newborn (מלא — כתובות מודעות ציבוריות, לא PII), 4_sigs/5_buyers/6_calls/7_deals (מצבי-ריק מעוצבים), 8_brief (מספרי המשרד 479/366/3697 — הכי שיווקי). סדר מומלץ לחנות: 8_brief, 2_home, 3_newborn, 4_sigs. הסקריפט: scratchpad/shoot.js.
- 2026-07-15: **צילומי iPad ל-App Store (נוצרו):** האפליקציה Universal (TARGETED_DEVICE_FAMILY=1,2) → אפל דורשת צילומי iPad. 8 צילומים ב-2064×2752 (iPad 13") ב-`~/Downloads/effie-appstore-screenshots-ipad`, אותה שיטת Playwright (viewport 1032×1376 @ DPR2, isMobile:false → נכנס לפריסת הדסקטופ של V2_DESKTOP_CSS: מגירת ניווט קבועה מימין). הכי טובים: 3_newborn (ממלא מצוין), 2_home. 8_brief ומצבי-ריק — עם whitespace בתחתית (התוכן מתוכנן לגובה טלפון) אך תקינים. סקריפט: scratchpad/shoot_ipad.js.
- 2026-07-16: **Sign in with Apple (דחיית App Store 4.8 — נבנה):** אפל דחתה את build 11 על 4.8 (Google בלי חלופה פרטית) + 3.2 (חשד לאפליקציה פנימית). ההחלטה: להוסיף Apple לצד Google (בקשת אייל). צד שרת (`app.py`): `/api/auth/apple` — אימות identityToken מול JWKS של אפל (PyJWT[crypto] ב-requirements; iss/aud/exp, aud=`APPLE_BUNDLE_ID` env או com.remaxfamily.familybot), מיפוי sub→טלפון ב-`apple_auth.json` (דיסק קבוע, כמו v2_gauth; שניהם נוספו ל-.gitignore) + `/api/auth/alink_request`+`alink_verify` — קישור ראשון באימות SMS (מקביל ל-glink; חשבון הביקורת עובד עם הקוד הקבוע). מסך הכניסה (`effie_v2.py`): כפתור שחור "המשך עם Apple" מעל Google, מוצג רק כשהפלאגין קיים על הגשר (build 12+); `finishLogin` משותף; ה-smsBox משרת גם את זרימת הקישור (ALINK). אומת: ast×2, node, unit JWT (5 תרחישי זיוף/תוקף ב-venv), integration מלא של alink עם חשבון הביקורת. **הבודק:** Sign in with Apple → קישור טלפון → 0501234567/1905.
- 2026-07-16: **החתמה מרובה מהתאמות + טיוטות טפסי החתמה (בקשת אייל, נבנה):** (1) **התאמת נכסים** (/v2/buyers): הכפתור הדביק "שלח X נכסים בוואטסאפ" פוצל לשניים חצי-חצי — ירוק "שלח X בוואטסאפ" + זהב "החתם על X נכסים" (`signSelected`) שפותח את /v2/sign?type=buyer עם הקונה וכל הנכסים המסומנים (v2signPre קיבל `props[]`; נכס יחיד נשאר `addr/price` לאחור). (2) **טיוטות**: אפשרות שלישית ב"אופן ההחתמה" — "טיוטא — הכנה לחתימה" (`MODE='draft'`), שומרת את מצב הטופס (לקוח/נכסים/עסקאות; רק שם חובה) דרך `POST /v2/api/sign/draft` → קונפיג `v2_sign_drafts` (_config_mutate, תקרת 100, פר-סוכן; admin רואה הכל). מוצגות במסך החתימות תחת **"הכל"** בלבד: צ'יפ "טיוטא" זהוב, "המשך לחתימה" (משחזר את הטופס המלא כולל deals דרך localStorage v2signDraft + DRAFT_ID) ופח מחיקה. חתימה/שליחה אמיתית מטיוטא מוחקת אותה אוטומטית (`clearDraft`). Endpoints: draft (save/update), drafts (list), draft_delete. אומת: ast, node×3 מסכים, integration מלא (שמירה/עדכון-בלי-כפילות/רשימה/דחיית-ריק/מחיקה/401; _save_config נכשל מקומית — סביבה בלבד, stub אימת את הלוגיקה), התנהגות selbar/signSelected ב-node (5 assertions). **לא נבדק live** — ממתין לפריסה.
- 2026-07-16: **🐞 החתמה מהתאמות לא מילאה את טלפון הקונה (דיווח אייל, תוקן):** ה-prefill לקח רק את `CUR_BUYER.phone` (שדה התצוגה) — קונים שאצלם התצוגה ריקה (הוזנו בלי פורמט) נשלחו לטופס בלי טלפון. תוקן: `buyerPhone()` — נפילה אחורה phone→tel→wa עם נרמול (972→0, הוספת 0 מוביל), משמש גם את signBuyer (בודד) וגם את signSelected (מרובה). אומת: ast+node+6 מקרי קצה של פורמט טלפון.
- 2026-07-16: **טיוטות החתמה ב"דורש טיפול" וביומן (בקשת אייל, נבנה):** הטיוטות (v2_sign_drafts) מוצגות עכשיו גם: (1) **בית** — /v2/api/sign/drafts נוסף ל-Promise.all של loadData; פריט "טיוטא להחתמה: <לקוח>" ב"דורש טיפול" עם אייקון עיפרון זהוב וצ'יפ "טיוטא", ord=2.5 (אחרי באיחור/היום/מחר, לפני עתידי-רחוק), לחיץ → goDraft → הטופס משוחזר. (2) **יומן ופולו-אפ** — כרטיס טיוטות תחת "הכל" בלבד (אחרי הבאיחור, לפני המתוזמנים), שורת mt לחיצה עם אותו goDraft; המונה כולל טיוטות רק ב"הכל". אומת: ast, node (בית+יומן), התנהגות מיון ה-care (פגישה→טיוטא→פולו-אפ עתידי).
- 2026-07-16: **עמוד החתימה הציבורי (/s/<token>) עוצב מחדש + תנאי שימוש לחותמים (בקשת אייל, בהשראת חתימה טובה — בלי להעתיק):** (1) **זרימת שני שלבים:** שלב 1 — קריאת ההסכם עם כפתור צף דביק "המשך לחתימה ›" (זהב, safe-area); שלב 2 — מסך חתימה נקי: ברכה בשם הלקוח, ת"ז (ולידציה קיימת), פאד חתימה (szPad נקרא בחשיפה — תיקון רוחב-0 בקנבס נסתר), צ'קבוקס הסכמה חובה + "חזרה לקריאת ההסכם". הכותרת (לוגו/סוכן/טבלת נכס) נשארת בשני השלבים. (2) **תמונת הסוכן** מהאפליקציה בטבעת זהב ליד פרטי הסוכן (/v2/api/avatar?p=, onerror מסתיר). (3) **פוטר אפי:** לוגו אוריגמי מיני + "מופעל על ידי אפי · תנאי שימוש ופרטיות" — גם במצב נחתם. (4) **`/sign-terms`** — דף תנאי שימוש ופרטיות לחותמים בניסוח מקורי של אפי (מבוסס על עקרונות הטקסט שאייל הדביק מחתימה טובה, כולל סעיף עלות עדות 10,000 ₪ — מנוסח מחדש; דין חיפה). ⚠️ מומלץ מעבר עו"ד על הנוסח. חוזה ה-API לא השתנה (/api/sign/complete: token/cid/signature + בדיקת agree בקליינט). אומת: ast, רינדור מלא דרך test client עם מסמך מדומה (11 בדיקות מבנה, pending+signed), node על ה-JS המחולץ, צילומי שני השלבים ב-Playwright.
- 2026-07-16: **כפתור "שלח ללקוח" על כל כרטיס חתימה (בקשת אייל, נבנה):** במסך החתימות, כל כרטיס עם קישור זמין מקבל כפתור וואטסאפ ירוק (#157A43, לפי מוסכמת הניגודיות): ממתינה → "תזכורת ללקוח" עם קישור החתימה (/s/<eid>); חתומה → "שלח ללקוח" עם קישור המסמך החתום (g.link). אין טלפון בשורות החתימה (מגבלה קיימת) → wa.me בלי מספר, נפתח לבחירת איש קשר עם ההודעה מוכנה. `sendSig` ליד delSig, אותו מיפוי _shown. אומת: ast+node+התנהגות שני המצבים.
- 2026-07-16: **ליטוש כפתורי הפעולות בחתימות (הערת אייל על העיצוב):** התוויות קוצרו — "צפייה במסמך (טרם נחתם)"→"צפייה במסמך", "צפייה במסמך החתום"→"המסמך החתום" (הסטטוס ממילא בכרטיס); `.acts .a` קיבל white-space:nowrap + פונט 12.5px — בלי שבירת שורות ליד כפתור הוואטסאפ.
- 2026-07-16: **שני תיקונים בעמוד החתימה הציבורי (דיווח אייל מבדיקה חיה):** (1) **תמונת הסוכן לא הופיעה** — התמונה של אייל דווקא קיימת בשרת (avatar 200); בבדיקה חתם "סוכן בדיקה" בלי תמונה וה-onerror העלים את העיגול. תוקן: fallback — עיגול נייבי עם האות הראשונה של שם הסוכן (כמו בסטורי), התמונה מתלבשת מעליו כשקיימת. הלקוח תמיד רואה זיהוי של הסוכן. (2) **⚡ 12 שניות מ"אשר וחתום" עד סיום** — sign/complete רץ כולו סינכרונית (3 קריאות Apps Script + פוש/SMS/WA) ואז reload שקורא שוב. תוקן באותו דפוס של send_remote: סינכרוני רק getsigndoc+updatesigndoc (השמירה הקובעת), השאר (updatesigning, קאש, התראות) ב-thread רקע; הקליינט מציג מסך "🎉 ההסכם נחתם ונשמר" מיד בלי reload, עם כפתור "צפייה במסמך החתום · שמירת PDF" (reload לפי דרישה — עד אז הרקע הסתיים). צפי: ~12ש' → ~4-6ש' עד אישור מיידי. אומת: ast+render (אות+okcard), סדר קריאות סינכרוני מינימלי + רקע מושלם ב-integration, node.
- 2026-07-16: **מספר רישיון תיווך באזור האישי → טפסי החתימה (בקשת אייל, נבנה):** שדה "מספר רישיון תיווך" באזור האישי (מתחת לפרופיל, מעל החלפת תמונה) — `GET/POST /v2/api/me/license`, נשמר בקונפיג `v2_licenses` {last9: lic} דרך _config_mutate (ספרות בלבד, עד 10; ריק=מחיקה; GET נופל אחורה לעמודת הרישיון בגיליון אנשי קשר). העמוד הציבורי /s/<token> מעדיף את הרישיון מהקונפיג על הגיליון — "רישיון תיווך מס׳: X" בתיבת הסוכן על כל טופס. אומת: ast×2, node, integration (שמירה עם ניקוי תווים 20-039→20039, קריאה, רינדור בעמוד הציבורי, מחיקה).
- 2026-07-16: **שני באגים מבדיקה חיה של החתימה (דיווח אייל, תוקנו):** (1) **"ממתין לחתימה" למרות שנחתם** — שורש: SIGNATURES_SOURCE=supabase, אבל עדכון הסטטוס (updatesigning) נכתב רק לגיליון דרך Apps Script; ל-Supabase זה מגיע רק בסנכרון התקופתי → הסטטוס באפליקציה תקוע. תוקן: `_post_sign_bg` מעדכן גם את Supabase ישירות — GET לפי `raw->>doc_token` (נפילה ל-`raw->>token`), merge של raw עם commission_pct=link (הקישור הוא מה שהופך את הכרטיס ל"נחתם") + event_id=ת"ז, PATCH, ואז ניקוי הקאש. אם השורה לא נמצאה — log.warning ("signatures row not found"). (2) **תמונת הסוכן לא מופיעה** — שורש: העמוד הציבורי חיפש טלפון רק בגיליון אנשי-קשר (fetch_agents_full), מקור לא אמין. תוקן: מועמדים מ-`_phones_for_name` (מפת חברים+אנשי קשר+config agents) + הגיליון, ונבחר **המספר שיש לו קובץ תמונה ב-v2_avatars** (בדיקת os.path.exists), לא הראשון. אומת: ast + integration (PATCH עם ה-raw הממוזג והקישור הנכון; בחירת הטלפון עם התמונה מבין שניים). ⚠️ אם אחרי פריסה עדיין תקוע — לבדוק בלוגים "signatures row not found" (אולי מפתח הטוקן ב-raw בשם אחר).
- 2026-07-16: **בלעדיות = שני הסכמים נפרדים בחתימה מרחוק (דרישה משפטית של אייל, נבנה):** בעל נכס עם בלעדיות: send_remote מפצל — טוקן+savesigndoc נפרדים למוכר ולבלעדיות. (1) **שרשור:** ה-SMS מקשר להסכם המוכר; מתחתיו מוצג "‏<בלעדיות> · חתימה ממתינה — ייפתח מיד לאחר חתימת ההסכם הזה" (מסגרת זהב מקווקוות); אחרי חתימת המוכר מסך ההצלחה מציג "המשך לחתימת הסכם הבלעדיות ›" → /s/<token2>. (2) **מספר ההסכם:** מספר המוכר (אותה נוסחת תצוגה מהטוקן) נשתל בגוף הבלעדיות בשורת `מספר ____` (regex `(מספר|מס')\s*_{2,}`, count=1; אם אין בלנק — log.warning) + תמיד בכותרת: "· להסכם מס' X". (3) **שורות הגיליון:** כל רשומת addsigning נושאת את הטוקן של ההסכם שלה (event_id נפרד) → סטטוס/תזכורת/קישור פר-הסכם במסך החתימות. חתימה במקום (submit) לא שונתה — עדיין חתימה אחת לזוג. אומת E2E: פיצול+שתילה+שרשור+event_ids+שני העמודים+שני מסכי ההצלחה (node eval). ⚠️ לוודא אחרי פריסה שנוסח תבנית הבלעדיות בקונפיג מכיל "מספר ___" (אחרת המספר רק בכותרת).
- 2026-07-16: **אזור אישי גלובלי (בקשת אייל):** האזור האישי (תמונה, רישיון תיווך, סנכרון יומן) נפתח עכשיו מעיגול הפרופיל בכל מסך, לא רק בבית — `v2Me()` ב-V2_BOOST (מוזרק לכל דף דרך _page): פאנל תחתון עצמאי (v2meWrap, z-index 400+, נתונים מ-localStorage fbName/fbRole/fbPhone/fbTok) עם העלאת תמונה (canvas 256), שדה רישיון (GET/POST /v2/api/me/license) וסנכרון Google. חיווט אוטומטי: `header .av` בלי onclick קיים מקבל click→v2Me (הבית שומר על openMe שלו; .av של כרטיסים ברשימות לא נתפס — הסלקטור מוגבל ל-header). טוסט עצמאי v2meToast. אומת: ast, node על BOOST, התנהגות v2Me (פאנל+אווטאר+רישיון), הזרקה ל-sigs/meets/buyers, הבית לא נדרס.
- 2026-07-16: **🐞 מספר ההסכם לא נשתל בסעיף 5 של הבלעדיות (בדיקה חיה של אייל):** שורש: נוסח הבלעדיות מכיל "מספר ____" פעמיים (מבוא + סעיף 5) וההחלפה הייתה count=1 — רק המבוא קיבל מספר. תוקן: החלפת כל המופעים + regex עמיד לתווי כיווניות נסתרים (‎/‏/‪-‮) ולקווים חלופיים (–—־). log.info עם מונה ההחלפות. אומת: 4 וריאציות של הבלנק בטקסט חוזה מציאותי. מהבדיקה החיה גם אומת: הכותרת "· להסכם מס' X" עובדת, האווטאר בעמוד עובד (סוכן בדיקה 5 עם תמונה), והשרשור מוכר→בלעדיות רץ.
- 2026-07-16: **🐞 דחיית App Store 2.1(a) — "Sign in with Apple yielded no further action" (iPad; תוקן בקוד השרת, בלי build חדש):** התקדמות — 3.2+4.8 התקבלו ונעלמו; נשאר באג טכני. שורש: שני חורי כשל-שקט ב-appleGo (מסך הכניסה מגיע מהשרת): (1) ה-`.catch` על authorize היה ריק; (2) הקריאה ל-/api/auth/apple בלי `.catch`. אם authorize נכשל/בוטל או השרת החזיר שגיאה — שום דבר לא קרה על המסך = "no further action". תוקן: (א) catch על authorize מבדיל ביטול (1001/cancel — שקט) משגיאה אמיתית (הודעה); (ב) catch על ה-fetch; (ג) כשל בלי identityToken/link מציג הודעה; (ד) מצב קישור-טלפון — הודעה כחולה ברורה "התחברת עם Apple — הזן טלפון" + scrollIntoView + focus; (ה) הכפתור disabled בזמן הזרימה. REASONS הורחב (apple_*, net, bad_token). ה-endpoint אומת חי (bad_token→PyJWT פעיל). **בלי build 14** — פריסת Render מספיקה; לענות לאפל + Resubmit. נותר: אימות חי על מכשיר של הזרימה המלאה (Apple→טלפון דמו 0501234567/1905).
- 2026-07-16: **🐞 כפילות חתימות ("נחתם" + "ממתין" לאותו הסכם — בדיקה חיה של אייל, מוכר+בלעדיות+קונים):** שורש: `_recent_signs_merge` דידדפ רק לפי (event_id, deal_type). כשהלקוח חותם, sign/complete משנה את event_id של השורה מהטוקן ל-ת"ז → הדדופ נשבר, ורשומת ה"נראות המיידית" (ממתין, לפי טוקן) נשארה יתומה ליד השורה האמיתית (נחתם). תוקן: דדופ יציב נוסף לפי (agent_canon, client_canon, deal_type) — לא משתנה בחתימה; רשומה מיידית מסוננת אם או-הטוקן או-המפתח-היציב תואם. גם: `send_remote` recs קיבלו `doc_token` (לצד event_id) → עדכון Supabase הישיר בחתימה מוצא את השורה (מבטל את "row not found" שהשאיר סטטוס תקוע). אומת: רפרודוקציה של תרחיש 3-ההסכמים (3 שורות פנימה→3 החוצה, בלי כפילות, נחתם גובר) + רגרסיה (דדופ לפני חתימה, גשר כשאין שורה).
- 2026-07-16: **🐞 המשך: "ממתין" למרות שהלקוח חתם (SMS אישר) — השורש האמיתי:** מקור החתימות ל-app הוא Supabase, אבל addsigning/updatesigning כותבים ל**גיליון**; Supabase מתמלא רק בסנכרון תקופתי → פער. גשר הנראות המיידית (_RECENT_SIGNS) הראה "ממתין" בלבד ולא התהפך בחתימה. **תובנה:** gunicorn=worker יחיד + 12 threads → הזיכרון משותף לכל הבקשות (כולל /api/sign/complete הציבורי). תוקן: `_recent_signs_mark_signed(token, link, cid)` — בחתימה מסמן את רשומת הגשר כ'נחתם' מיד (commission_pct=link, event_id=ת"ז, רענון ts), סינכרוני ב-sign/complete. הסוכן רואה "נחתם" תכף, בלי להמתין לסנכרון. יחד עם הדדופ היציב (196dbb0) — אין כפילות כשהשורה האמיתית נוחתת. אומת: תרחיש 3 הסכמים (מוכר+בלעדיות+קונים) — כולם→נחתם מיד, ואפס כפילות כשה-real נוחת. ⚠️ הצילום של אייל ב-20:25 היה כנראה לפני פריסת 196dbb0; הפריסה הבאה נושאת גם את זה וגם את mark_signed.
- 2026-07-16: **כפתור "נתק חשבון Apple" באזור האישי (כדי לבדוק את מסלול הבודק):** אייל לא הצליח לבדוק את זרימת Apple כי ה-Apple ID שלו כבר מקושר → נכנס ישר בלי שלב הטלפון (מה שדווקא מוכיח ש-Apple עובד למשתמש חוזר). נוסף: `_apple_unlink_sub(sub)` ב-app.py + `POST /v2/api/me/apple_unlink` (מסיר את כל ה-subs הממופים לטלפון המשתמש) + כפתור "נתק חשבון Apple (לבדיקה מחדש)" ב-v2Me (iOS בלבד). אחרי ניתוק, ההתחברות הבאה עם Apple מתחילה מסלול משתמש-חדש (Apple→טלפון 0501234567/1905) — בדיוק מה שהבודק רואה. אומת: ast×2, node, integration (מסיר רק את ה-subs של המשתמש, לא של אחרים).
- 2026-07-16: **מיתוג App Store עודכן (החלטת אייל):** Name: "אפי · העוזר של המתווך" → **"אפי · נדל״ן חכם"** (רמז ל-AI, קצר). Subtitle → **"כל המשרד בכיס — CRM חכם לנדל״ן"**. תיאור/keywords עודכנו לכלול AI/בינה-מלאכותית/חכם. מטא-דאטה בלבד ב-App Store Connect — לא נוגע בקוד/build. (שים לב: המפרט המקורי ב-remax-family/CLAUDE.md אומר תת-כותרת "העוזר של המתווך" — אייל שינה מדעת.)
- 2026-07-16: **הגנה כפולה בצד לקוח נגד כפילות חתימות (הכפילות חזרה בבדיקת אייל — כנראה תיקוני השרת 196dbb0/52aef6e טרם נפרסו; הצילום זהה להתנהגות טרם-תיקון):** נוסף דדופ ב-render של /v2/sigs — מקבץ לפי (client|address|kindOf); אם לקבוצה יש גם נחתם וגם ממתין → משאיר רק את הנחתמים (חתימות עצמאיות בסטטוס זהה נשארות). זו הגנה שעובדת גם אם גשר-השרת מפספס. אומת: תרחיש אייל (קונים כפול→נחתם יחיד, מוכר+בלעדיות נשמרים) + רגרסיה (כתובות שונות לא נמחקות). ⚠️ עדיין לוודא שהפריסה כוללת את 196dbb0+52aef6e (mark_signed) כדי שגם ה"נחתם המיידי" יעבוד — הדדופ מסתיר כפילות אך לא הופך "ממתין"→"נחתם" בלי ה-mark_signed.
- 2026-07-16: **🐞 "ממתין למרות שחתם" — השורש האמיתי (מוכר+קונים תקועים, רק בלעדיות נחתם):** לא בעיית פריסה — באג במיזוג. `_recent_signs_mark_signed` סימן את הגשר כ'נחתם', אבל `_recent_signs_merge` העדיף את שורת-האמת (עדיין 'ממתין' מ-Supabase שלא סונכרן) וזרק את הגשר החתום → 'ממתין' הוצג. (בלעדיות הראה נחתם רק כי שורת האמת שלו כבר סונכרנה.) תוקן: המיזוג בונה מפת signed_link מהגשר ו**משדרג** שורת-אמת ממתינה ל'נחתם' כשהגשר אומר שנחתם (לפי stablekey/tokkey), לצד הוספת רשומות-גשר בלי שורת-אמת. אומת: תרחיש אייל (3 חתומים, בלעדיות סונכרן ומוכר/קונים לא → כולם נחתם, בלי כפילות) + 3 רגרסיות (אין-גשר/גשר-ממתין-לא-מזייף/גשר-בלבד).
- 2026-07-19: **🐞 "נכס חם לא מסומן באפליקציה" (דיווח אייל):** שורש — `loadHot` טוען רק את הנכסים החמים של המשתמש הנוכחי (`agent_phone=eq.<שלי>`), אבל מנהל/מתאמת רואים ב"שלי" את נכסי כל הסוכנים (MINE_MULTI); נכס שסוכן אחר סימן חם מופיע בבריף (כלל-משרדי) אבל הכפתור לא נדלק (מסונן לטלפון של המנהל). תוקן: (1) `GET /v2/api/hot?all=1` — למנהל/מתאמת בלבד — מחזיר נכסים חמים כלל-משרדיים + מפת `byAgent` (key→שם סוכן). (2) `loadHot` שולח `?all=1` כש-MINE_MULTI, שומר `HOT_BY`, ומנסה שוב עד 2 פעמים בכשל (מבטל את הכשל-השקט שהיה יכול להשאיר כפתורים כבויים אחרי תקלת רשת/מכסה). (3) הכפתור מציג "נכס חם · בבריף (שם הסוכן)" בתצוגת מנהל. אומת: ast+node+integration (מצב all=1 + byAgent). ⚠️ ה-toggle של מנהל על נכס של סוכן אחר עדיין פועל על הטלפון של המנהל (מקרה קצה קיים) — לא נגעתי, ההצגה תוקנה.
- 2026-07-19: **🐞 יומן שימוש — סוכן מופיע פעמיים (דיווח אייל: "יאיר"/"מנהל" ×2):** שורש — `v2_api_usage_today` צבר פעימות לפי `phone`, וסוכן עם כמה מספרים (רגיל+וירטואלי) קיבל שורה לכל מספר, והזמן התפצל. תוקן: איחוד לפי שם-הסוכן (`_canon_key`, מפתח `n:<canon>`); בלי שם — נפילה לטלפון (`p:<phone>`). כל הפעימות של סוכן (כל מספריו) מתאחדות לשורה אחת עם הזמן המצטבר. אומת: ast + unit (יאיר בשני טלפונים + מנהל בשניים → 2 שורות, יאיר=3 פעימות מאוחדות).
- 2026-07-19: **🐞 כפילות בלעדיות בשליחה (אלי שמול שלח בלעדיות → הופיעה פעמיים):** הדדופ הקודם (47b6765) איחד רק "נחתם+ממתין"; שתי בלעדיות **ממתינות** (הרגע נשלחו — שליחה כפולה/פיצול/גשר+גשר) נשארו שתיהן. תוקן בשני צדדים: (1) **לקוח** (/v2/sigs) — לכל מפתח (לקוח+כתובת+kindOf) שומרים נציג אחד בלבד: נחתם אם קיים, אחרת הראשון — מקריס גם ממתין+ממתין. (2) **שרת** (`_recent_signs_merge`) — רשומות הגשר עוברות דדופ גם ביניהן לפי stablekey (added_stable), לא רק מול שורות האמת. אומת: server (2 בלעדיות ממתינות→1) + client (2 בלעדיות→1, מוכר נשמר). מקרה קצה נדיר (שתי בלעדיות לגיטימיות לאותו לקוח+כתובת) — מוצג נציג אחד; זניח.
- 2026-07-19: **עריכת מחיר קונה + תיקון כפילות קונה מהחתמה (בקשות אייל):** (1) **עריכת תקציב** — בגיליון עריכת הקונה נוסף שדה "תקציב" (עם פירמוט פסיקים חי, fmtBudget) לצד הטלפון; saveEdit שולח `budget` ב-/api/buyers/update; app.py מוסיף `budget` ל-`_up` של updatebuyer (עמודה 'תקציב'). ⚠️ תלוי בכך ש-Apps Script updatebuyer תומך בעמודת budget — לוודא שנשמר (load() מרענן; אם חוזר ריק, לנתב אחרת). (2) **כפילות קונה** — `_add_buyer_from_signing`: הדדופ היה ב-try/except שבלע כשל קריאה → נוצר כפיל בזמן תקלת Supabase. תוקן: אם קריאת הקונים נכשלת — **מדלגים על ההוספה** (לא יוצרים כפיל) + ניסיון חוזר עם קאש נקי; והתאמה עכשיו לפי טלפון **או שם** (טלפון בפורמט שונה לא יוצר כפיל). אומת: ast×2, node, integration (מיזוג לפי טלפון; דילוג-יצירה בכשל קריאה).
- 2026-07-19: **מסך TV (/v2/tv?k=) responsive לאייפון (בקשת אייל — אותו קישור בלי כניסה גם בטלפון):** הקישור כבר עבד (200, kiosk בלי כניסה) אבל הפריסה רוחבית-לטלוויזיה נחתכה בפורטרייט (הכותרת בלטה, גריד 4 עמודות). נוסף `@media (max-width:820px)`: כותרת ממורכזת עוטפת-שורות, גריד מונים 2×2, כרטיס נכס חם עמודה-אחת, אווטאר סוכן קטן, פונטים מוקטנים (h1 26px, stat 38px, ttl 26px). אותו מסך/תוכן/לופ — רק מסתדר לגובה. אומת: ast+node + רינדור מקומי עם דמה ב-390px (נקי, בלי חיתוך). CSS בלבד — אפס שינוי לוגיקה/דאטה.
- 2026-07-19: **🐞 "רק 27 נכסים בשת"פ" (דיווח אייל):** אי-סימטריה — במסך הנכסים בלי חיפוש, `api_search_properties` (משרד) מחזיר את *כל* הפעילים (471), אבל `api_search_exclusives` (שת"פ) חתך `rows[:30]` → 30 (27 אחרי dedupeShtaf בצד הלקוח). הבריכה גדולה מ-30. תוקן: הוסר החיתוך — "כל הבלעדיות" מחזיר את כל השת"פ הממויין מהחדש לישן, כמו המשרד. (מסלול החיפוש-עם-מילים נשאר scored[:15] — top matches, לא נגעתי.) אומת: ast+import.
- 2026-07-19: **תאריך כניסת נכס בכרטיס (בקשת אייל):** הנתון כבר הגיע לקליינט (`p.date` — משרד: "תאריך יצירה"; שת"פ: received_at) אך לא הוצג. נוסף `propDate(p)` — מפרמט ISO/DD-MM ל-DD/MM/YYYY + זמן יחסי ("היום"/"אתמול"/"לפני X ימים"/"לפני X חודשים"); שורת "נכנס: ..." אפורה קטנה מתחת למחיר בכרטיס. נופל יפה (raw כמו-שהוא אם לא ניתן לפרסר; ריק→בלי שורה). אומת: ast+node+התנהגות (ISO/DD-MM/יחסי/ריק/זבל).
- 2026-07-19: **TV במובייל — תיאור גולש + אין ניווט (דיווח אייל):** (1) `.dsc` בלי clamp גלש מתחת ל-body overflow:hidden ודלף על הפוטר → במדיה מובייל נוסף `-webkit-line-clamp:5` + text-align:center + פונט 15px. (2) הסטורי התקדם רק בטיימר (טלוויזיה) — במובייל אין דילוג. נוסף `goto(delta)` (מאפס TIMER) + הקשה על חצי-מסך (ימין RTL=אחורה, שמאל=קדימה) + שני כפתורי חץ עגולים `.tvNav` (מוסתרים מעל 820px, מופיעים במגע; cursor:auto הוחזר). אומת: ast+node+רינדור 390px (אפס גלישה אופקית, 2 חצים גלויים, clamp פעיל). טלוויזיה (>820px) ללא שינוי.
